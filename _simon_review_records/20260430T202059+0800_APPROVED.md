# Simon QC APPROVED — ShiftOps Calendar

checked_at: 2026-04-30T20:20:59+08:00
verdict: APPROVED
responsibility: none
active_product_lock: PM_OP_QC_REPAIR_LOOP / 20260428_shift_scheduler_web_supershift_style
source_task_path: /home/sport/WORK/AGENTS/04_打回修改/sebastian/20260428_shift_scheduler_web_supershift_style
d_package_path: /mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style

## 固定流程證據
- 已重讀 ACTIVE_PRODUCT_LOCK，僅處理 active product `20260428_shift_scheduler_web_supershift_style`。
- 已讀 PRODUCT_SPEC.md、README.md、TEST_RESULT.md、SPEC.md、ACCEPTANCE.md、FULL_BUILD_CHECKLIST.md、RC.md、NEXT_STEP.md、TASK_META.json。
- 已啟動 production `node --run start` at `http://localhost:3000`，並做 live route/API/browser/auth/export probes。

## Build / Test evidence
- `node --run build`: exit 0 PASS；stdout 僅有 Next.js `DYNAMIC_SERVER_USAGE` dynamic-route warnings for `/api/calendar/google-fallback`, `/api/data/holidays`, `/api/calendar/ics`，production live probes 對應 endpoint 皆通過。
- `node --run test:unit`: exit 0, 8 suites / 98 tests PASS。
- `node --run test:api`: exit 0, 10 suites / 159 tests PASS；console.error 為測試內刻意 mock DB error negative path，assertions PASS 且命令乾淨退出。
- `node --run test:e2e`: exit 0, 1 suite / 11 tests PASS；embedded `next start` lifecycle clean。

## Production live / browser evidence
- Page routes HTTP 200: `/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/billing`, `/audit`, `/more`, `/share-print`.
- Browser `/login` form with `manager@shiftops.local / manager2026` navigated to `/calendar`; console `js_errors=[]`; browser `fetch('/api/auth/me', credentials include)` returned 200 with MANAGER session.
- API auth: `POST /api/auth/login` 200 with Set-Cookie; bearer `/api/auth/me` 200 for `manager@shiftops.local`.
- Core workflow: AI preview 200 with `previewToken`, preview GET 200, apply-preview 200 with `applyRunId` and `beforeSnapshot`, rollback 200 success.
- Calendar/data APIs 200: projection, day-inspector, google-fallback, last-known, holidays, staff, shift-types, swap-requests.
- Export/download: PDF job status/download 200 `application/pdf` `%PDF`; PNG job status/download 200 `image/png` PNG magic; ICS job status/download 200 `text/calendar` `BEGIN:VCALENDAR`; direct `/api/calendar/ics` 200 `text/calendar`.

## Spec / checklist / D package
- FULL_BUILD_CHECKLIST raw recount: `[x]=67`, `[ ]=0`, P0 unchecked=0.
- PRODUCT_SPEC route-table pages covered by production HTTP probes; auth/session/export/download covered beyond public 200 routes.
- D package executable contents present: `package.json`, `app/`, `public/`, `.next/`, truth-pack metadata.
- Source BUILD_ID now `MDb203J17s3GkEz7Dgsjr`; D package BUILD_ID `7o11N6N3VzyhPH4Uhgxg7`. Difference is QC-induced by this clean build rerun after OP package sync; D executable package was present and current metadata is re-synced below.

## Verdict
APPROVED. No remaining resubmit conditions.
