# Simon QC Verdict — REJECTED

- task_id: `20260428_shift_scheduler_web_supershift_style`
- reviewed_at: `20260430T130654+0800`
- active_lock: `PM_OP_QC_REPAIR_LOOP / 20260428_shift_scheduler_web_supershift_style`
- source_task_path: `/home/sport/WORK/AGENTS/04_打回修改/sebastian/20260428_shift_scheduler_web_supershift_style`
- d_package_path: `/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style`

## Verdict

REJECTED / OP_DELIVERY_DEFECT

## 必修項

1. 修復 standalone `node --run test:api`：目前新增/既有 export download API 測試不可重現，4 個 `/api/export/jobs/[jobId]/download` cases 失敗（PDF/ICS/PNG/CSV 均 404）。
2. 完成或逐項正式證據收斂 `FULL_BUILD_CHECKLIST.md` 剩餘 `35` 個未勾選項；不得在 `TEST_RESULT.md` 仍標示 `all_must_fix_completed=false` / `ready_for_build_ready=false` 時送審。
3. 同步 Windows-visible D 槽 final-review package 的 truth-pack metadata 與 build artifacts；目前 D package 仍落後於 source truth pack。
4. 重新提供 clean build/test/live/browser/API/export/D freshness 證據後再 resubmit。

## 實測證據

### 1. 正式文件 / gate

- 已讀 `PRODUCT_SPEC.md`：規格第 19/20/21/29 節要求 test/build gate、Script A–N、export download endpoint、D package freshness。
- 已讀 `README.md`：公開驗收入口要求 `node --run build && node --run start`，API 驗收包含 export job/status。
- 已讀 `TEST_RESULT.md`：line 8–9 明確為 `all_must_fix_completed: false`、`ready_for_build_ready: false`；line 21–26 承認 checklist `[x]=32`, `[ ]=35`，產品仍 not ready。
- 獨立核對 `FULL_BUILD_CHECKLIST.md`：`[x]=32`, `[ ]=35`。

### 2. Clean API test gate

命令：`for p in 3000 3101; do fuser -k ${p}/tcp || true; done && node --run test:api`

結果：exit_code=1。

失敗摘要：

- `GET /api/export/jobs/[jobId]/download › PDF download returns 200`：Expected 200, Received 404。
- `GET /api/export/jobs/[jobId]/download › ICS download returns 200`：Expected 200, Received 404。
- `GET /api/export/jobs/[jobId]/download › PNG download returns 200`：Expected 200, Received 404。
- `GET /api/export/jobs/[jobId]/download › CSV download returns 200`：Expected 200, Received 404。
- Summary：Test Suites `1 failed, 5 passed`; Tests `4 failed, 86 passed, 90 total`。

### 3. Build / live probes

- `node --run build`：exit_code=0，production build PASS。
- `node --run start` 後 route probes：`/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/billing`, `/audit`, `/more`, `/share-print` 均 HTTP 200。
- Auth cookie probe：`POST /api/auth/login` 回 HTTP 200 並設 `shiftops_session; Secure; HttpOnly; SameSite=lax`；同 cookie jar `GET /api/auth/me` 回 HTTP 200 manager payload。
- Browser login probe：以瀏覽器開 `/login`，填 `manager@shiftops.local / manager2026` 後 click 登入，成功導至 `/calendar`，console/js_errors 為 0。
- Documented Script M style preview body `prompt/startDate/endDate/mockMode`：本輪 live probe 回 HTTP 200 並產生 `previewToken`，此項比上一輪改善。
- Export live probe：建立 ICS job → status 宣告 `/api/export/jobs/<jobId>/download` → live download HTTP 200 `text/calendar`。但 clean test gate 仍 fail 4 cases，代表測試/route handler contract 不可重現，不能覆蓋 API test gate。

### 4. D 槽 freshness

比對 source 與 D final-review package：

- `TEST_RESULT.md`：source mtime `1777523757.7694683`, D mtime `1777522190.8054676`。
- `RC.md`：source mtime `1777524738.3962626`, D mtime `1777522190.8104877`。
- `TASK_META.json`：source size `45594`, D size `33946`。
- `.next/BUILD_ID`：source current build `-MVmLHOt-upMalaMZ5nLu`（本輪 QC clean build 後產生），D package `BSNBmvcaqp-byLP5wUp5o`；此差異部分受 QC 重跑 build 影響，但 D truth-pack metadata 明確未同步最新 source。

## 責任歸屬

OP_DELIVERY_DEFECT

## 退回對象

退回 OP / Sebastian。

## Resubmit 條件

1. `node --run test:api` 在清空 3000/3101 ports 後 exit_code=0，且無 export download 404、server lifecycle、EADDRINUSE、open handle 或 force-exit 掩蓋問題。
2. `FULL_BUILD_CHECKLIST.md` 剩餘 `35` 項完成或逐項以正式證據收斂；`TEST_RESULT.md` / `RC.md` / `TASK_META.json` 的 `all_must_fix_completed` 與 `ready_for_build_ready` 必須同步為真且有證據支撐。
3. D 槽 `/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style` 重新同步 source truth pack 與可執行 build artifacts，提供 BUILD_ID/manifest/metadata freshness evidence。
4. 重新跑 clean `node --run build`、`node --run test:unit`、`node --run test:api`、`node --run test:e2e`、production route probes、browser login/calendar hydration、auth cookie、Script M、export download probes，全部附證據後再送 `build.ready`。
