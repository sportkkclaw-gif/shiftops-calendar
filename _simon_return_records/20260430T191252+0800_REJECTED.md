# Simon QC Verdict — REJECTED

checked_at: 2026-04-30T19:12:52+08:00
product_id: 20260428_shift_scheduler_web_supershift_style
active_task_path: `/home/sport/WORK/AGENTS/04_打回修改/sebastian/20260428_shift_scheduler_web_supershift_style`
verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP / sebastian
next_event: null

## 必修項
1. 完成或正式 evidence-reconcile `FULL_BUILD_CHECKLIST.md` 剩餘未勾項：D38, I75, D38（目前 `[x]=64`, `[ ]=3`, P0 unchecked=0）。
2. D38：月曆缺班/coverage badge 必須消費 persisted `CoverageAlert` / formal projection API source，不可只停留 UI heuristic。
3. I75：落地 `AI_SOLVER_SPEC.md` 的 solver interface + deterministic constraint scoring pipeline，或提供 PM-approved scope reconciliation；目前 checklist 仍標未完成。
4. J84：不拆工/完整 release gate 必須收斂為完成狀態，`all_must_fix_completed=true` / `ready_for_build_ready=true` 前不得送 `build.ready`。
5. 重新刷新 Windows-visible D 槽 final-review package：`/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style`，使 executable build、truth-pack metadata、BUILD_ID/manifest 與 source 最新狀態一致。

## 實測證據
- ACTIVE_PRODUCT_LOCK 已遵守：`PM_OP_QC_REPAIR_LOOP` / active product `20260428_shift_scheduler_web_supershift_style`。
- 已讀正式文件：`PRODUCT_SPEC.md`、`README.md`、`TEST_RESULT.md`、`FULL_BUILD_CHECKLIST.md`、`RC.md`、`NEXT_STEP.md`、`TASK_META.json`。
- Current source gate flags：`TASK_META.status=returned_for_fix`, `all_must_fix_completed=false`, `ready_for_build_ready=false`, `next_event=null`。
- Checklist raw recount（寫入前重讀）：`[x]=64`, `[ ]=3`, P0 unchecked=0; remaining labels: D38, I75, D38。
- Independent tests：delegate probe `node --run test:unit` exit 0（72/72 PASS）；`node --run test:api` exit 0（159/159 PASS，僅 expected/handled DB-error console noise）。
- QC clean build：`node --run build` exit 0；只見 Next.js `DYNAMIC_SERVER_USAGE` warnings for `/api/calendar/ics`, `/api/calendar/google-fallback`, `/api/data/holidays`，build 完成。
- QC clean e2e：先清除 QC live server 後重跑 `node --run test:e2e` exit 0（11/11 PASS），server startup/stop clean。
- Production live probes：`next start` 後 spec route table `/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/billing`, `/audit`, `/more`, `/share-print` 全部 HTTP 200。
- Browser auth probe：真瀏覽器 `/login` 使用 `manager@shiftops.local / manager2026` 成功進入 `/calendar`；browser `fetch('/api/auth/me', credentials:'include')` 回 200。Python cookiejar 對 local HTTP + Secure cookie 回 401，依 skill 規則視為工具差異，不作單獨 auth fail。
- API/export smoke：`/api/calendar/ics` 回 200 `text/calendar` 且 body starts `BEGIN:VCALENDAR`; `/api/calendar/last-known` 回 200 JSON。`/api/calendar/google-fallback` unauthenticated 回 401，需以登入/授權情境補齊正式 evidence。
- D 槽 package freshness：D package exists at `/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style` but stale. D checklist `[x]=56`, `[ ]=11` vs source `[x]=64`, `[ ]=3`; source BUILD_ID `lc6eTi5q8MIgXweNzXZFq`, D BUILD_ID `parAT-eIqBEY54_wJEnax` mismatch. D `TEST_RESULT.md` / `RC.md` / `NEXT_STEP.md` / `TASK_META.json` still older than source truth pack.

## 判斷假完成風險
- 本輪不是可放行狀態：truth pack 自身仍誠實標示 `all_must_fix_completed=false` / `ready_for_build_ready=false`，且 checklist 仍有 3 個未完成項。
- 即使 build/tests/live/browser 多數通過，未完成 checklist + D package stale 仍違反 final review gate；不得 APPROVED。

## resubmit 條件
1. OP 完成 D38/I75/J84 或取得 PM 明確 scope reconciliation，並把 `FULL_BUILD_CHECKLIST.md` 每項證據同步為最新。
2. `TEST_RESULT.md` 必須重跑並記錄 clean `node --run test:unit && node --run test:api && node --run test:e2e && node --run build`，且不可只沿用舊結果。
3. 重跑 production live/browser/API/export/PWA probes，含登入授權下 Google fallback/export download evidence。
4. 刷新 `/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style`，使 metadata counts、BUILD_ID/manifest 與 source 一致。
5. 僅在上述條件完成後，才可設定 `all_must_fix_completed=true`, `ready_for_build_ready=true` 並送 `build.ready`。
