# Simon QC Verdict — REJECTED

generated_at: 2026-04-30T18:02:14+08:00
generated_by: simon-qc-cron
active_product_lock: respected (`PM_OP_QC_REPAIR_LOOP`, active product only)
verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP / sebastian
next_event: null

## 必修項
1. 完成或正式逐項 reconciliation `FULL_BUILD_CHECKLIST.md` 目前仍未勾的 11 項，並補 item-level evidence。
2. 將 `all_must_fix_completed` 與 `ready_for_build_ready` 只在 checklist / evidence / D package 全同步後改為 true；目前仍為 false，不可送 `build.ready`。
3. 重新整理 Windows-visible final-review package：`/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style`，同步 source、truth-pack metadata、`.next/BUILD_ID` 與 manifest。
4. resubmit 前重跑 clean `node --run build`, `node --run test:unit`, `node --run test:api`, `node --run test:e2e`，並保留 production live/browser/auth/export evidence。

## 實測證據
- `PRODUCT_SPEC.md` / `README.md` / `TEST_RESULT.md` 已讀；active lock 指定 task path 符合本輪驗收對象。
- `node --run build`（經 `/tmp/shiftops_qc` symlink）exit_code=0；stdout 僅記錄 `/api/data/holidays`, `/api/calendar/google-fallback`, `/api/calendar/ics` 的 `DYNAMIC_SERVER_USAGE` warning，build 完成。
- `node --run test:unit` exit_code=0：2 suites / 19 tests PASS。
- `node --run test:api` exit_code=0：9 suites / 150 tests PASS；DB error-path console.error 為測試內預期錯誤路徑。
- `node --run test:e2e` exit_code=0：1 suite / 11 tests PASS；embedded Next server lifecycle clean。
- Production `node --run start` live route probes：`/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/billing`, `/audit`, `/more`, `/share-print` 全部 HTTP 200。
- Browser login：`/login` 使用 `manager@shiftops.local / manager2026` 成功導向 `/calendar`；browser `fetch('/api/auth/me', credentials=include)` = 200；console/js_errors empty。
- curl cookie jar auth：login 回 `Set-Cookie: shiftops_session=...; Secure; HttpOnly; SameSite=lax`；`curl -b` `/api/auth/me` = 200 / role MANAGER。Python urllib 因 Secure cookie + local HTTP 未送 cookie 產生 401，依 skill 規則不作單獨失敗依據。
- Protected/live APIs：calendar projection、day inspector、Google fallback、last-known、ICS 均 HTTP 200，ICS 回 `BEGIN:VCALENDAR`。
- AI preview/apply/rollback corrected contract：preview 200 取得 token；apply 200 取得 applyRunId；rollback 以 README 指定 body (`applyRunId` + `organizationId`) 200。
- Export probes：PDF download 200 `%PDF-1.4`; PNG download 200 `image/png` magic bytes; ICS download 200 `text/calendar` / `BEGIN:VCALENDAR`。
- `FULL_BUILD_CHECKLIST.md` final raw count before write: checked=56, unchecked=11, P0 unchecked=0。
- Current unchecked checklist items:
- L23: - [ ] 命名制度只作 seed，可調整 phase/天數/班別/組別 offset。→ `NamedPatternSeed` model 存在 (`schema.prisma:253-259`) 但無 UI/API 調整機制；`seed.ts` 只建立 seed。Blocker: "No API/UI to adjust named pattern phase/days/shift/group offset"
- L26: - [ ] 可處理技能/資格/角色覆蓋 constraint。→ `parseIntent` 無技能/資格解析；`buildCalendarProjection` 不考慮 `StaffSkillCertification`；Mock AI 固定 staff assignment。Blocker: "parseIntent skill/certification constraint parsing not implemented"
- L38: - [ ] 缺班警告內嵌在月曆。→ `CoverageAlert` model 存在；Mock AI 產生 `coverageAlerts` (lines 125-135)；但月曆 UI 無 badge 渲染 logic。Blocker: "CoverageAlert model+seed exists; badge rendering in calendar UI not implemented"
- L42: - [ ] 加班 assignment 不覆蓋原班別並寫入 audit。→ Mock AI `buildCalendarProjection` 僅顯示 OT candidates 不取代 OFF shifts；但 `/api/ai/apply-preview` 未持久化 beforeSnapshotRef 且未寫入 AI action AuditEvent。Blocker: "OT assignment audit persistence missing in apply-preview flow"
- L64: - [ ] AI conversations 留存。→ 無 `AICONversation` model；`AuditEvent` model 存在但只用於 swap/apply 事件。Blocker: "No AICONversation model in schema.prisma"
- L75: - [ ] `AI_SOLVER_SPEC.md` 實作完成。→ Spec file (106 行) 存在；`lib/mock-ai.ts` 實作 parseIntent + buildCalendarProjection；但 `solve()` 函式/SolverInput interface 未實作；Constraint scoring pipeline 未實作（只做 mock）。Blocker: "Solver pipeline (sections 2-5 of AI_SOLVER_SPEC.md) not implemented beyond mock; deterministic solver missing"
- L78: - [ ] `RBAC_MATRIX.md` 權限測試通過。→ Spec file (48 行) 存在；`lib/rbac.ts` 實作基礎 RBAC；但 matrix 測試 (section 4) 無獨立測試檔；member AI scope / manager cross-location blocking 未驗證。Blocker: "No standalone RBAC matrix test suite; lib/rbac.ts basic logic exists but full matrix not tested"
- L79: - [ ] `PRISMA_SCHEMA_SPEC.md` 對應 migration 完成。→ Spec file (336 行) 存在；`schema.prisma` 包含所有 spec models；但無 migrations folder (使用 `db push`)；需確認 `prisma migrate deploy` 可達到同樣 state。Blocker: "No migrations/ folder; db push used instead of migrate; production deployment migration path not verified"
- L84: - [ ] 不拆工、不分批交付。→ 待處理：命名制度 offset 調整 UI (C23)、技能/資格 constraint 解析 (C26)、月曆 UI badge 渲染 (D38)、OT assignment beforeSnapshotRef 持久化 (D42)、AICONversation model (H64)、RBAC matrix 獨立測試套件 (I78)、migration deploy path 驗證 (I79)、Calendar page 串接 projection API (J86)、SolverPreview 共享 contract (J87)。已實作：calendar projection API、day-inspector、coverage-summary、reports analytics。Blocker: "8 genuine feature gaps remain; 6 previously-flagged items now resolved"
- L86: - [ ] Calendar UI 直接讀 projection，不自行推算班別。→ Calendar page (`/app/calendar/page.tsx`) 存在但無 `/api/calendar/projection` route 讀取；目前直接呼叫 `/api/ai/preview-schedule` 當 projection source。Blocker: "Calendar page reads from /api/ai/preview-schedule, not a dedicated calendar projection API"
- L87: - [ ] SolverPreview 同時供 AI、calendar、apply、audit 使用。→ `SolverPreview` type 存在 (`lib/mock-ai.ts`)；但不同 consumer 之間未建立共享 contract；AI preview 結果不自動同步到 calendar projection。Blocker: "SolverPreview consumed by apply-preview and rollback; calendar projection has separate/duplicated computation"
- D package executable content exists, but freshness is stale: metadata/build mismatches = none; source BUILD_ID `parAT-eIqBEY54_wJEnax`, D BUILD_ID `parAT-eIqBEY54_wJEnax`. Source BUILD_ID drift is QC-induced by this clean build, but D truth-pack metadata is also stale vs source and remains a delivery blocker.

## 判斷假完成風險
- 技術 gate 本輪為綠，但正式交付 gate 仍未達：`all_must_fix_completed=false`、`ready_for_build_ready=false`，且 checklist 仍有 11 個未勾項。
- D 槽待最終審核 package 未同步最新 truth-pack metadata / PRODUCT_SPEC / checklist，Windows-visible final-review package freshness 不合格。
- 因 OP/TEST_RESULT/RC 已誠實標示未 ready，本輪不是 `review.done`，維持 `returned_for_fix` / `next_event=null`。

## resubmit 條件
1. 11 個未勾 checklist 項目全部完成或以 PM/OP 正式 item-level evidence 收斂，且 raw checklist 與 TASK_META/RC/TEST_RESULT counts 一致。
2. `all_must_fix_completed=true`、`ready_for_build_ready=true` 僅在上述條件成立後同步更新。
3. D 槽 final-review package 與 source 最新成品同步；至少 metadata、PRODUCT_SPEC、FULL_BUILD_CHECKLIST、TEST_RESULT、RC、NEXT_STEP、TASK_META、`.next/BUILD_ID`/manifest freshness 可核對。
4. OP 重新送 `build.ready` 前提供 clean build/test/live/browser/auth/export evidence。
