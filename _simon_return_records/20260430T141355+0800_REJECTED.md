# Simon QC Verdict — 20260428_shift_scheduler_web_supershift_style

verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP / Sebastian
updated_at: 20260430T141355+0800
active_product_lock: PM_OP_QC_REPAIR_LOOP / 20260428_shift_scheduler_web_supershift_style

## 必修項
1. 修復 clean `node --run build`：目前 TypeScript compile 失敗於 `app/api/ai/report/coverage/route.ts:85`，production build gate 不成立。
2. 修復 clean `node --run test:unit`：目前 2 suites / 19 tests fail，測試啟動 `tests/api/setup.ts` 嘗試等待 `http://localhost:3000/` 30s 未 ready；unit test gate 不可重現。
3. 完成或正式逐項 evidence/reconcile `FULL_BUILD_CHECKLIST.md` 剩餘 29 項；不得在 `TEST_RESULT.md` 仍標示 `all_must_fix_completed=false` / `ready_for_build_ready=false` 時送 build.ready。
4. 最終 resubmit 前重新同步 Windows-visible D 槽 package；目前 D package truth metadata/checklist 仍舊版，且 D checklist 為 `[x]=32, [ ]=35`，source checklist 為 `[x]=38, [ ]=29`。
5. 修復後重新跑 clean build/unit/api/e2e/live/browser/auth/Script-M/export probes，並把命令、exit code、route/API/browser 證據同步到 TEST_RESULT/RC/TASK_META。

## 實測證據
- 已先讀取 ACTIVE_PRODUCT_LOCK：mode=`PM_OP_QC_REPAIR_LOOP`，active_product_id 與 active_task_path 均為本任務；未處理任何非 active task。
- 已讀 `PRODUCT_SPEC.md`、`README.md`、`TEST_RESULT.md`、`TASK_META.json`、`RC.md`、`NEXT_STEP.md`、`FULL_BUILD_CHECKLIST.md`。
- `TEST_RESULT.md` 仍標示：`task_status=returned_for_fix`、`all_must_fix_completed=false`、`ready_for_build_ready=false`、`next_event=null`。
- Source checklist raw count：`FULL_BUILD_CHECKLIST.md` `[x]=38`, `[ ]=29`。
- Clean build：先清 port 3000，從 `/tmp/shiftops_qc_active` 執行 `node --run build` → exit_code=1；重跑 `node --run db:generate && node --run build` 仍 exit_code=1。錯誤：
  - `./app/api/ai/report/coverage/route.ts:85:65`
  - `Type error: Type '{ include: { shiftType: { select: { id: true; name: true; durationMinutes: true; }; }; }; }' does not satisfy the constraint 'boolean | ShiftAssignmentDefaultArgs<DefaultArgs> | null | undefined'.`
- 因 production build 失敗，未能啟動可信的 clean `next start` production server；production live route/browser gate 不成立。
- `node --run test:unit` → exit_code=1；`tests/unit/mock-job-store.test.ts` 與 `tests/unit/core.test.ts` 均失敗，19/19 tests fail；共同錯誤為 `Server at http://localhost:3000/ did not become ready within 30000ms`，來源 `tests/api/setup.ts:43`。
- D 槽 package 檢查：`/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style` 存在且有 `package.json/app/.next`，但 metadata/freshness 不同步：D `PRODUCT_SPEC.md` mtime=20260430T125217+0800、D `TEST_RESULT.md`=20260430T130746+0800、D `FULL_BUILD_CHECKLIST.md`=20260430T113526+0800、D checklist `[x]=32, [ ]=35`；source 最新 `PRODUCT_SPEC.md`=20260430T135818+0800、`TEST_RESULT.md`=20260430T134831+0800、source checklist `[x]=38, [ ]=29`。

## 判定
- build gate、unit test gate、truth-pack release gate、D 槽 freshness gate 均未通過。
- 本輪不寫 `review.done`，不送 build.ready；`next_event` 保持 `null`。

## resubmit 條件
1. `node --run build` clean exit_code=0（含 Prisma generate 後仍可重現）。
2. `node --run test:unit`、`node --run test:api`、`node --run test:e2e` clean exit_code=0，且無 timeout/open handle/EADDRINUSE/server lifecycle 警訊。
3. `FULL_BUILD_CHECKLIST.md` 剩餘 29 項完成或逐項正式 evidence 收斂，並同步 `TEST_RESULT.md all_must_fix_completed=true`、`ready_for_build_ready=true`。
4. D 槽 final-review package 重新部署並與 source metadata、BUILD_ID/manifest/checklist counts 對齊。
5. 重新提供 production route probes、browser login/cookie auth、Script-M preview、export job download/ICS 的最新證據。
