# Simon QC Verdict — 20260428_shift_scheduler_web_supershift_style

verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP / Sebastian
updated_at: 20260430T135334+0800
active_product_lock: PM_OP_QC_REPAIR_LOOP / 20260428_shift_scheduler_web_supershift_style

## 必修項
1. 在 `FULL_BUILD_CHECKLIST.md` 剩餘 29 個未完成項目完成或以正式 item-level evidence 收斂前，不得送 `build.ready` / final review。
2. `TEST_RESULT.md` 目前誠實標示 `all_must_fix_completed=false`、`ready_for_build_ready=false`；OP 必須完成剩餘 checklist 並同步 `TEST_RESULT.md`、`RC.md`、`NEXT_STEP.md`、`TASK_META.json` 後才可重送。
3. D 槽 Windows-visible final-review package 必須重新同步最新 truth-pack metadata 與最終 OP build/package freshness evidence；不得只保留舊版 metadata。
4. 保持本輪已通過 regression：unit/api/e2e/build、production route probes、browser login、curl cookie auth、Script-M preview、export job download、ICS fallback，不得回退。

## 實測證據
- Active lock 已確認：`/home/sport/WORK/AGENTS/00_系統設定/ACTIVE_PRODUCT_LOCK.json` 指向本 active product，mode=`PM_OP_QC_REPAIR_LOOP`。
- 規格/文件已讀：`PRODUCT_SPEC.md`、`README.md`、`TEST_RESULT.md`、`FULL_BUILD_CHECKLIST.md`、`ACCEPTANCE.md`、`TASK_META.json`。
- Raw checklist count：`FULL_BUILD_CHECKLIST.md` = `[x]=38`, `[ ]=29`。
- `TEST_RESULT.md` gate：`all_must_fix_completed=false`、`ready_for_build_ready=false`、`task_status=returned_for_fix`。
- Clean tests/build（本輪 QC 重跑）：
  - `node --run test:unit` → exit_code=0，19/19 PASS。
  - `node --run test:api` → exit_code=0，90/90 PASS。
  - `node --run test:e2e` → exit_code=0，11/11 PASS。
  - `node --run build` → exit_code=0；build output 有 Next dynamic server usage diagnostic for `/api/calendar/google-fallback` / `/api/calendar/ics` during static generation，但最終 build 成功且 route classified dynamic。
- Production `next start` live probes（localhost:3000）：spec route table `/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/billing`, `/audit`, `/more`, `/share-print` 全部 HTTP 200。
- Browser login：Browser 開 `/login`，輸入 `manager@shiftops.local / manager2026` 後成功進 `/calendar`；console/js_errors = 0；calendar 可見 A/B 班、缺口、加班 overlay UI。
- Auth probe：`POST /api/auth/login` HTTP 200；`curl -c/-b` cookie jar 呼叫 `/api/auth/me` HTTP 200；Bearer `/api/auth/me` HTTP 200。Python `http.cookiejar` 因 local HTTP + Secure cookie 不送 cookie 而 401，已用 curl/browser 排除 false negative。
- Script-M preview：documented body `{prompt,startDate,endDate,mockMode}` → HTTP 200，`previewToken` present，status=`partial`，`calendarProjection_len=31`，`overtimeCandidates_present=true`。
- Export/ICS：建立 ICS export job HTTP 200；status endpoint advertises `downloadUrl=/api/export/jobs/<jobId>/download`；download endpoint HTTP 200 且 body starts `BEGIN:VCALENDAR`；direct `/api/calendar/ics?...` HTTP 200 且 body starts `BEGIN:VCALENDAR`。
- D 槽 package：`/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style` 存在且含 `package.json/app/lib/public/.next`，但 D 槽 metadata 與 source 最新 truth pack 不一致：D `FULL_BUILD_CHECKLIST.md` size=3402 vs source=12773，D `TEST_RESULT.md` size=1695 vs source=1880，D `RC.md`/`TASK_META.json` mtime/內容仍停在較早 QC 記錄。Source `.next/BUILD_ID` 是本輪 QC 重跑 build 後產生，故 BUILD_ID 差異不作單獨 defect；metadata freshness 仍未同步。

## 判定
即使核心 live probes 與 regression tests 本輪通過，正式 gate 仍明確 not ready：`TEST_RESULT.md` 自述 not ready，且 checklist 仍有 29 項未完成。依 quality-verdict 規則，OP 已誠實標示 `all_must_fix_completed=false` / `ready_for_build_ready=false` 時，QC 不得 APPROVED。

## 責任歸屬
OP_DELIVERY_DEFECT

## 退回對象
退回 OP / Sebastian。

## Resubmit 條件
1. 完成或正式證據收斂剩餘 29 個 unchecked checklist items，且逐項附 evidence path / command / probe result。
2. 同步 `FULL_BUILD_CHECKLIST.md`、`TEST_RESULT.md`、`RC.md`、`NEXT_STEP.md`、`TASK_META.json`，使 `all_must_fix_completed=true`、`ready_for_build_ready=true` 有 item-level evidence 支撐。
3. 重新同步 `D:\WORK\成品區\待最終審核\sebastian\20260428_shift_scheduler_web_supershift_style`，包含最新 metadata 與 Windows-visible executable product package freshness evidence。
4. 重跑並記錄 clean `node --run test:unit` / `test:api` / `test:e2e` / `build`、production route probes、browser login/auth cookie、Script-M preview、export job download/ICS probes。
5. 僅當上述 gate 全部一致 ready 後，才可送 `build.ready`。
