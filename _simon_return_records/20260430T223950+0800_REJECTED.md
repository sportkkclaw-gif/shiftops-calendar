# Simon QC Verdict — REJECTED

- task_id: `20260428_shift_scheduler_web_supershift_style`
- reviewed_at: `2026-04-30T22:39:50+08:00`
- verdict: `REJECTED`
- responsibility: `OP_DELIVERY_DEFECT`
- return_to: `OP / Sebastian`
- active_lock: `PM_OP_QC_REPAIR_LOOP / 20260428_shift_scheduler_web_supershift_style`

## 必修項
1. OP 必須重新部署 Windows 可見正式待最終審核成品包到 `D:\WORK\成品區\待最終審核\sebastian\20260428_shift_scheduler_web_supershift_style`。
2. 該 package 必須包含可執行 Web/Next.js 產品內容（至少 `package.json`、`app/`/`src`、`public`、`.next` 或等價 production package），不可只有 truth-pack metadata。
3. 重新部署後必須同步 source 與 D 槽 package 的 `RC.md`、`NEXT_STEP.md`、`TEST_RESULT.md`、`TASK_META.json`，並提供 D package freshness evidence（BUILD_ID/manifest/mtime/hash）。
4. OP 需重送 `build.ready`；QC 才可從 `待最終審核` 正式入口再驗收。

## 實測證據
- ACTIVE_PRODUCT_LOCK 已讀取並符合 active product：`20260428_shift_scheduler_web_supershift_style`。
- 已讀 `PRODUCT_SPEC.md`、`README.md`、`TEST_RESULT.md`、`FULL_BUILD_CHECKLIST.md`、`RC.md`、`TASK_META.json`。
- Source clean build：`node --run build` exit_code=0；僅出現 Next.js `DYNAMIC_SERVER_USAGE` warning（`/api/calendar/ics`、`/api/data/holidays`、`/api/calendar/google-fallback`），相關 endpoint 已 live-probed。
- Unit tests：`node --run test:unit` PASS，8 suites / 98 tests，exit_code=0。
- API tests：`node --run test:api` PASS，10 suites / 159 tests，exit_code=0；error-path console output 為測試內預期 DB error-path。
- E2E tests：`node --run test:e2e` PASS，1 suite / 11 tests，exit_code=0。
- Production server：`node --run start` on `http://localhost:3000` ready。
- PRODUCT_SPEC page routes live probe 全部 HTTP 200：`/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/data/patterns`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/audit`, `/billing`, `/more`, `/share-print`。
- Browser form login：`/login` 使用 `manager@shiftops.local / manager2026` 成功導向 `/calendar`；browser console 無 JS errors。
- Auth/API live probes：bearer `/api/auth/me` HTTP 200；curl cookie/local auth probe `/api/auth/me` HTTP 200。
- Core workflow probes：AI preview HTTP 200、apply HTTP 200 且回 `beforeSnapshot`、rollback HTTP 200。
- Export/fallback probes：ICS export job status/download HTTP 200 且 body 為 `BEGIN:VCALENDAR`；direct `/api/calendar/ics` HTTP 200；Google fallback HTTP 200；last-known schedule HTTP 200。
- Checklist raw count：`[x]=67`, `[ ]=0`, P0 unchecked=`0`。
- Source BUILD_ID after QC build：`dhDn8DeyV5r4aFiswrX63`。
- Blocking D evidence：正式 final-review package 不存在：`/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style`；`/mnt/d/WORK/成品區/待最終審核/sebastian` 目前未找到 `20260428_shift_scheduler_web_supershift_style`。
- `_退回修改` 僅有歷史 package：`/mnt/d/WORK/成品區/_退回修改/sebastian/20260428_shift_scheduler_web_supershift_style`，BUILD_ID `7o11N6N3VzyhPH4Uhgxg7`，不是正式 `待最終審核` 入口，不能作為放行依據。

## 判斷
Source 技術 gate 本輪可通過，但 Windows-visible final review package 已從正式 `待最終審核` 入口撤回/缺失。依 quality-verdict D 槽驗收規則，正式入口缺失不可 APPROVED。

## 責任歸屬
`OP_DELIVERY_DEFECT` — OP 交付/部署到 D 槽正式待審入口缺失。

## 退回對象
退回 OP / Sebastian。

## Resubmit 條件
- fresh package 出現在 `D:\WORK\成品區\待最終審核\sebastian\20260428_shift_scheduler_web_supershift_style`。
- OP 更新 source 與 D package truth-pack metadata，證明 BUILD_ID/manifest/package freshness。
- OP 重跑並記錄 build/unit/api/e2e/live/browser/auth/export evidence。
- 重送 `build.ready` 後再進入 Simon QC。
