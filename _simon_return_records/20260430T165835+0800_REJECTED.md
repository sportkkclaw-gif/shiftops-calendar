# Simon QC REJECTED — 20260428_shift_scheduler_web_supershift_style

generated_at: 2026-04-30T16:58:35+08:00
generated_by: simon-qc-cron
verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP / sebastian
active_product_lock: PM_OP_QC_REPAIR_LOOP / 20260428_shift_scheduler_web_supershift_style

## 必修項
1. 完成或正式收斂 `FULL_BUILD_CHECKLIST.md` 仍未勾選的 21 項（目前 `[x]=46`, `[ ]=21`, P0 unchecked=0），並逐項提供證據；不得只以 build/test pass 取代產品完整性 gate。
2. 將 truth pack 同步為可送驗狀態：`all_must_fix_completed=true`、`ready_for_build_ready=true` 必須有 checklist/TEST_RESULT/RC 證據支撐；目前仍為 false，不能送 `build.ready`。
3. 重新同步 Windows-visible final review package `/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style`，包含 source、truth pack metadata、`.next/BUILD_ID`/manifest；D package 目前仍保留較舊 truth pack（TEST_RESULT/RC/TASK_META 約 16:00），未包含本輪 16:35 後 source truth pack 狀態。
4. 保留並重跑本輪已恢復通過的 clean gates：`node --run build`、`node --run test:unit`、`node --run test:api`、`node --run test:e2e`、production `node --run start` route/API/browser probes。

## 實測證據
- ACTIVE_PRODUCT_LOCK 已遵守：只處理 active task `/home/sport/WORK/AGENTS/04_打回修改/sebastian/20260428_shift_scheduler_web_supershift_style`。
- `PRODUCT_SPEC.md`、`README.md`、`TEST_RESULT.md` 已讀；現有 TEST_RESULT 仍標示 `task_status=returned_for_fix`, `all_must_fix_completed=false`, `ready_for_build_ready=false`, `simon_verdict=REJECTED`。
- 使用 ASCII symlink `/tmp/shiftops_qc_active` 執行，避免中文 workdir tool block。
- `node --run build` → exit_code=0，PASS；stdout 仍有 Next.js `DYNAMIC_SERVER_USAGE` warnings for `/api/calendar/ics` (`request.url`) 與 `/api/calendar/google-fallback` (`request.headers`)，但 build 完成且相關 live probe 可另行驗證。
- `node --run test:unit` → exit_code=0，2 suites / 19 tests PASS。
- `node --run test:api` → exit_code=0，8 suites / 135 tests PASS，無 timeout/open-handle failure。
- `node --run test:e2e` → exit_code=0，1 suite / 11 tests PASS；embedded Next server start/stop 正常。
- Production `node --run start` live routes：`/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/audit`, `/billing`, `/share-print`, `/more` 均回 200 HTML。
- Browser login：`/login` 以 `manager@shiftops.local / manager2026` 登入後導向 `/calendar`；browser `fetch('/api/auth/me',{credentials:'include'})` 回 200，角色 MANAGER；頁面顯示月曆、班別、人員、缺口/衝突/加班 overlay 控制，無 uncaught JS error。
- Protected browser API probes：`/api/calendar/google-fallback?...` 回 200；`/api/calendar/projection?organizationId=org_demo&locationId=loc_demo&month=2026-05` 回 200；`/api/calendar/day-inspector?...date=2026-05-04` 回 200。
- AI core flow corrected probe：preview 200 → apply 200 → rollback 200（rollback body 需 `applyRunId` + `organizationId`）。
- Export probes：PDF/PNG/ICS job create 200；status advertises download URL；download 200 with `application/pdf`, `image/png`, `text/calendar` and ICS begins `BEGIN:VCALENDAR`。
- Source `.next/BUILD_ID` after QC clean build: `AWkwi6R_hd-RGsrCJyHlQ`. D package `.next/BUILD_ID`: `D91_FkNzIZJ2l2IDqrhxc`. BUILD_ID drift is QC-induced by this round's source rebuild, but D truth-pack metadata remains older than current source truth pack and must be refreshed before resubmit。
- D package executable contents exist at `/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style` (`package.json`, `app/`, `public/`, `.next/` present); D checklist counts: [x]=46, [ ]=21, P0 unchecked=0。

## 判斷假完成風險
- 本輪 build/test/live/browser/API 已恢復通過，表示上一輪 duplicate declaration/server readiness regression 已被 OP 修復。
- 但正式 checklist 仍有 21 個未完成項，且 truth pack 自身仍誠實標示 `all_must_fix_completed=false` / `ready_for_build_ready=false`。依 quality-verdict 規則，即使 clean probes 全通，也不得 APPROVED。
- Windows-visible D package truth pack 未同步到最新 source truth pack，final review package freshness gate 未完成。

## Verdict
REJECTED / OP_DELIVERY_DEFECT

## 退回對象
OP / sebastian

## Resubmit 條件
1. 完成或正式 reconcile 目前 21 個 checklist 未勾項，並在 `FULL_BUILD_CHECKLIST.md`、`TEST_RESULT.md`、`RC.md`、`TASK_META.json` 寫入逐項證據。
2. 將 `all_must_fix_completed` 與 `ready_for_build_ready` 改為 true 前，必須能由 checklist counts 與實測證據支持；不可只改 summary/gate 欄位。
3. 重新同步 `/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style` 的 source + truth pack + `.next` artifact，並記錄 BUILD_ID/manifest freshness。
4. Resubmit 前重新跑並附上 clean `node --run build`, `test:unit`, `test:api`, `test:e2e`, production route table, browser login/cookie auth, AI preview/apply/rollback, export job/download, ICS/Google fallback probes。
5. 僅在上述全部成立後才可送 `build.ready`；目前 `next_event` 必須維持 null。
