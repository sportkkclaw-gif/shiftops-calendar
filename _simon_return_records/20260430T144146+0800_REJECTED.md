# Simon QC Verdict — 20260428_shift_scheduler_web_supershift_style

generated_at: 2026-04-30T14:41:46+08:00
verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP / Sebastian
active_lock: PM_OP_QC_REPAIR_LOOP / active product only

## 必修項
1. 完成或正式逐項 reconcile `FULL_BUILD_CHECKLIST.md` 剩餘 26 項，並把 `TEST_RESULT.md` / `RC.md` / `TASK_META.json` gate 同步到 `all_must_fix_completed=true`、`ready_for_build_ready=true` 後才可 resubmit。
2. 重包並同步 D 槽 Windows-visible final-review package：`/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style`，需包含最新 source metadata、checklist counts、TEST_RESULT/RC/TASK_META 與 build artifact evidence。
3. OP 需補齊目前 checklist 明列未完成功能/證據：Holiday/Lunar model/seed、AI preference memory、DuPont/Pitman/Panama/skill constraint、calendar projection/Day Inspector/overtime APIs、AI reports、AI conversation/audit、solver/projection/overtime/RBAC spec gates 等。
4. resubmit 前重跑並記錄 clean build/unit/api/e2e、production live route、browser login、auth cookie、AI preview/apply/rollback、export job/download、PWA/offline/fallback probes。

## 實測證據
- ACTIVE_PRODUCT_LOCK 已讀取並符合：`active_product_id=20260428_shift_scheduler_web_supershift_style`，`active_task_path=/home/sport/WORK/AGENTS/04_打回修改/sebastian/20260428_shift_scheduler_web_supershift_style`，`mode=PM_OP_QC_REPAIR_LOOP`。
- `PRODUCT_SPEC.md` / `README.md` / `TEST_RESULT.md` 已讀；`TEST_RESULT.md` 仍誠實標示 `task_status=returned_for_fix`、`all_must_fix_completed=false`、`ready_for_build_ready=false`。
- Raw checklist count：source `FULL_BUILD_CHECKLIST.md` = `[x]=41`, `[ ]=26`；此為 hard gate 未達，不能 APPROVED。
- Clean command evidence（QC 2026-04-30 14:35–14:36）：
  - `node --run build` exit_code=0；build 期間有 Next dynamic-server-usage warnings for `/api/calendar/google-fallback` and `/api/calendar/ics`，最終仍 exit 0。
  - `node --run test:unit` exit_code=0；2 suites / 19 tests PASS。
  - `node --run test:api` exit_code=0；7 suites / 104 tests PASS。
  - `node --run test:e2e` exit_code=0；1 suite / 11 tests PASS；embedded Next server ready and stopped cleanly。
- Production live route probe（`next start` on localhost:3000）：PRODUCT_SPEC required pages all returned 200: `/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/billing`, `/audit`, `/more`, `/share-print`。
- Browser probe：real browser opened `/login`, submitted `manager@shiftops.local / manager2026`, navigated to `/calendar`; calendar rendered shift cells and overtime overlay controls; browser console only logged `SW registered`, no JS errors observed。
- Auth/API probes：`POST /api/auth/login` status 200 with `Set-Cookie: shiftops_session=...; Secure; HttpOnly; SameSite=lax`; same curl jar `GET /api/auth/me` status 200。
- AI/export probes：
  - `POST /api/ai/preview-schedule` documented-compatible body status 200, `previewToken` present。
  - `POST /api/ai/apply-preview` with `previewToken+organizationId+locationId` status 200, returned `applyRunId` and `beforeSnapshot`; rollback status 200。
  - `GET /api/calendar/ics?...` status 200 and body begins `BEGIN:VCALENDAR`。
  - `GET /api/calendar/google-fallback?...` status 200。
  - `GET /api/calendar/last-known?...` status 200。
  - `POST /api/export/jobs` type ICS status 200; status query exposes download URL; `GET /api/export/jobs/<jobId>/download` status 200, `content_type=text/calendar`, body begins `BEGIN:VCALENDAR`。
- D 槽 final-review package side-check：package exists at `/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style`, but metadata is stale/mismatched: source checklist `[x]=41, [ ]=26` vs D checklist `[x]=32, [ ]=35`; D `TEST_RESULT.md`, `RC.md`, `TASK_META.json`, `NEXT_STEP.md` older/smaller and missing latest PM/QC reconciliation. Source `.next/BUILD_ID` drift from this QC build is not used as an independent defect; metadata/checklist freshness mismatch alone blocks release。

## 判斷假完成風險
- 本輪不是單純假完成：OP truth pack 目前仍標示 not ready (`all_must_fix_completed=false`, `ready_for_build_ready=false`)。
- 但若在此狀態送 final review/build.ready，會構成假完成；26 個 checklist 未完成項與 D 槽 metadata stale 是 release blocker。

## verdict
REJECTED / OP_DELIVERY_DEFECT

## resubmit 條件
- `FULL_BUILD_CHECKLIST.md` 剩餘 26 項完成或正式收斂並具逐項 evidence。
- `TEST_RESULT.md` / `RC.md` / `TASK_META.json` gate 與 checklist counts 一致，且 ready flags 全部達成。
- D 槽 final-review package 重新打包，source 與 D metadata/checklist/build evidence 一致。
- clean build/unit/api/e2e/live/browser/auth/export/PWA probes 全部重跑並寫入 TEST_RESULT。
