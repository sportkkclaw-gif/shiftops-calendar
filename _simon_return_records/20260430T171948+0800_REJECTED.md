# Simon QC Verdict — 20260430T171948+0800

verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP / sebastian
active_product_id: 20260428_shift_scheduler_web_supershift_style
active_task_path: /home/sport/WORK/AGENTS/04_打回修改/sebastian/20260428_shift_scheduler_web_supershift_style

## 必修項
1. 完成或正式以 item-level evidence 收斂 `FULL_BUILD_CHECKLIST.md` 剩餘 unchecked items；目前 source raw count: `[x]=46`, `[ ]=21`, P0 unchecked=0。
2. 將 `TEST_RESULT.md` / `TASK_META.json` 的 `all_must_fix_completed` 與 `ready_for_build_ready` 只在真實達標後改為 true；目前仍為 false，不得送 `build.ready`。
3. 重新同步 Windows-visible D 槽 final-review package：`/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style`，確保 truth-pack metadata、`.next/BUILD_ID`、manifest 與 source 最新狀態一致。
4. Resubmit 前重跑並記錄 clean build/unit/api/e2e、production route、browser login/cookie auth、AI preview/apply/rollback、export job/download、ICS/google fallback/PWA probes。

## 實測證據
- ACTIVE_PRODUCT_LOCK 已遵守：只處理 active product `20260428_shift_scheduler_web_supershift_style`。
- `node --run build` → exit_code=0 PASS；16 個 PRODUCT_SPEC routes compiled。
- `node --run test:unit` → exit_code=0，2 suites / 19 tests PASS。
- `node --run test:api` → exit_code=0，9 suites / 150 tests PASS；有 1 個非阻斷 console.error（PATCH `/api/ai/preferences` 測試預期錯誤路徑）。
- `node --run test:e2e` → exit_code=0，1 suite / 11 tests PASS。
- Production route probes：`/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/billing`, `/audit`, `/more`, `/share-print` 均 HTTP 200。
- Browser login：`/login` 以 `manager@shiftops.local / manager2026` 登入後導向 `/calendar`；browser `fetch('/api/auth/me', {credentials:'include'})` 回 200。
- Curl cookie auth：`POST /api/auth/login` 回 200 並設 `shiftops_session; Secure; HttpOnly; SameSite=lax`；同 cookie jar `GET /api/auth/me` 回 200 / MANAGER。
- Export/ICS smoke：export job create/status 可用；技術 gate 本輪為 green。
- Formal gate：source `FULL_BUILD_CHECKLIST.md` 仍 `[x]=46`, `[ ]=21`，`TEST_RESULT.md` 仍 `all_must_fix_completed=false` / `ready_for_build_ready=false`。
- D package：存在 executable contents，但 D `.next/BUILD_ID=D91_FkNzIZJ2l2IDqrhxc`，source after QC rebuild `.next/BUILD_ID=BNNW7jhOIwWgm2hfO5zUr`，不一致；D checklist count `[x]=46`, `[ ]=21`；stale metadata candidates: TEST_RESULT.md, RC.md, NEXT_STEP.md, TASK_META.json。

## 判斷假完成風險
技術 probes 已通過，但 formal release gate 仍明確標示未完成；若此狀態下送 `build.ready` 或要求 final approval，會形成 checklist/ready flags/D-package freshness 假完成風險。依 quality-verdict 規則，ready flags false + unchecked checklist items + D package freshness mismatch 不可 APPROVED。

## 責任歸屬
OP_DELIVERY_DEFECT。

## 退回對象
退回 OP / Sebastian。

## resubmit 條件
- `FULL_BUILD_CHECKLIST.md` 剩餘 21 項全部完成或由 PM/OP 以正式 evidence path / command / probe result 收斂；不得口頭完成。
- `TEST_RESULT.md`、`RC.md`、`NEXT_STEP.md`、`TASK_META.json` 同步為真實 gate 狀態，且 `next_event` 僅在 dispatcher 事件需要時使用；未達標保持 `null`。
- D 槽 final-review package 重新打包並同步 source truth pack + build artifacts；source/D `BUILD_ID`/manifest/metadata freshness 有證據。
- 重跑 clean build/unit/api/e2e 與 production/browser/API/export/PWA probes，將結果寫回 `TEST_RESULT.md` 後再 resubmit。
