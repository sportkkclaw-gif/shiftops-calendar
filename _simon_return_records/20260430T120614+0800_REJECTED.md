# Simon QC Verdict — REJECTED

task_id: 20260428_shift_scheduler_web_supershift_style  
checked_at: 20260430T120614+0800  
verdict: REJECTED  
responsibility: OP_DELIVERY_DEFECT  
return_to: OP / Sebastian  
active_lock: PM_OP_QC_REPAIR_LOOP matched active_product_id and active_task_path

## 必修項
1. 完成或正式收斂 `FULL_BUILD_CHECKLIST.md` 剩餘 35 個未勾項，補逐項證據；在此之前不得宣稱 build.ready/final approve。
2. 修正 Auth 契約：`PRODUCT_SPEC.md` 指定 `JWT httpOnly cookie + RBAC`，login 後 cookie-only `/api/auth/me` 必須在 README 指定的 `http://localhost:3000` 驗收入口可用；瀏覽器 `/login` 表單也必須實際送出並導向受保護/產品頁。
3. 保持已通過 gate：clean `node --run test:unit`、`node --run test:api`、`node --run test:e2e`、`node --run build`、production route probes、API preview/apply/rollback、export/ICS probes。
4. 重新同步 `/home/sport/WORK/AGENTS` truth pack 與 `D:\WORK\成品區\待最終審核\sebastian\20260428_shift_scheduler_web_supershift_style`，含最新 `RC.md`、`NEXT_STEP.md`、`TASK_META.json`、`TEST_RESULT.md`、`FULL_BUILD_CHECKLIST.md` 與 production build artifacts。

## 實測證據
- 已讀：`ACTIVE_PRODUCT_LOCK.json`、`PRODUCT_SPEC.md`、`README.md`、`TEST_RESULT.md`、`FULL_BUILD_CHECKLIST.md`、`ACCEPTANCE.md`、`RC.md`、`NEXT_STEP.md`。
- `TEST_RESULT.md` 自承：`all_must_fix_completed=false`、`ready_for_build_ready=false`；`task_status=returned_for_fix`、`next_event=null`。
- `FULL_BUILD_CHECKLIST.md` 實際仍為 `[x]=32`、`[ ]=35`，包含 `.env.example`、Prisma migrations/seed、Holiday/Lunar seed、coverage requirements、AI preference memory、多制度自然語言、calendar cell/inspector/AI report、AI audit/rollback、P0 補件與整合核查等未完成項。
- Clean verification 本輪重跑：
  - `node --run test:unit` → exit_code=0，19/19 PASS。
  - `node --run test:api` → exit_code=0，83/83 PASS。
  - `node --run test:e2e` → exit_code=0，11/11 PASS。
  - `node --run build` → exit_code=0，PASS（build 期間仍輸出 Next.js dynamic server usage diagnostics for `/api/calendar/google-fallback` and `/api/calendar/ics`，但未造成 build fail）。
- Production `next start` live route probes：`/`, `/login`, `/calendar`, `/ai-assist`, `/swap-requests`, `/export`, `/export/history`, `/reports`, `/settings`, `/audit`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/share-print`, `/billing`, `/more` 全部 HTTP 200。
- API positive probes：
  - AI preview → HTTP 200，`previewToken` returned，`calendarProjection` length=31。
  - Apply preview → HTTP 200，returned `applyRunId` and non-null `beforeSnapshot`。
  - Rollback → HTTP 200 success。
  - Export PDF/PNG/ICS create + query → HTTP 200，requested type preserved；ICS endpoint → HTTP 200 `text/calendar` and `BEGIN:VCALENDAR`。
- Auth/browser negative probes：
  - `PRODUCT_SPEC.md` lines 197-198 / API table require `JWT httpOnly cookie + RBAC` and login issuing httpOnly cookie/JWT.
  - `POST /api/auth/login` returns `set-cookie: shiftops_session=...; Secure; HttpOnly; SameSite=lax` plus bearer token, but cookie-only `GET /api/auth/me` on both `http://localhost:3000` and `http://127.0.0.1:3000` returned 401 `Authentication required`; bearer `/api/auth/me` returned 200. Cookie contract is therefore not independently reproducible through the documented HTTP local entry.
  - Browser opened `/login`; no JS console error, but filling `manager@shiftops.local / manager2026` and clicking Login / pressing Enter produced no API request, no cookie, and no navigation.
- D package pre-verdict freshness check：`D:\WORK\成品區\待最終審核\sebastian\20260428_shift_scheduler_web_supershift_style` exists and key files matched source before this QC verdict write (`TEST_RESULT.md`, `FULL_BUILD_CHECKLIST.md`, `RC.md`, `NEXT_STEP.md`, `TASK_META.json`, `.next/BUILD_ID`).

## 判定
REJECTED / OP_DELIVERY_DEFECT。

雖然本輪 build/tests/live route/API export/AI flow 多數正向探針通過，但 OP truth pack 仍誠實標示 not ready，且 35 個 checklist 項未完成/未正式收斂；另新增驗收發現 Auth cookie/browser login 契約與 PRODUCT_SPEC 不一致。依 quality-verdict 規則，未達 build.ready/final approve gate，不得放行。

## resubmit 條件
1. OP/Sebastian 完成或正式收斂剩餘 35 個 checklist 項並補逐項證據；`TEST_RESULT.md`、`FULL_BUILD_CHECKLIST.md`、`RC.md`、`NEXT_STEP.md`、`TASK_META.json` 不得互相矛盾。
2. 修復/正式收斂 Auth：cookie-only protected-session probe (`login` -> same cookie jar `/api/auth/me`) 必須可重現；瀏覽器 `/login` 表單必須可登入或規格/README 正式改成 bearer-only 並補相應驗收證據。
3. 重新 clean 跑 `node --run test:unit`、`node --run test:api`、`node --run test:e2e`、`node --run build`，再跑 production route table、browser console、auth cookie、AI preview/apply/rollback、export PDF/PNG/ICS/ICS download probes。
4. 同步 D 槽 Windows-visible 成品包與最新 source truth pack 後重新 resubmit `build.ready`。
