# Simon QC Verdict — 20260428_shift_scheduler_web_supershift_style

verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP / Sebastian
updated_at: 20260430T154626+0800
active_product_lock: confirmed PM_OP_QC_REPAIR_LOOP / 20260428_shift_scheduler_web_supershift_style

## 必修項
1. 完成或正式逐項 evidence/reconcile `FULL_BUILD_CHECKLIST.md` 剩餘 21 項；source raw counts 仍為 `[x]=46`, `[ ]=21`。
2. 同步 truth-pack gate：`TEST_RESULT.md` / `TASK_META.json` 仍標示 `all_must_fix_completed=false`、`ready_for_build_ready=false`，不得送 build.ready/review.done。
3. 修復或提供正式證據收斂 audit persistence / D42 類缺口；目前 checklist 仍保留未完成 P0/Phase 9 gate。
4. 重包並同步 Windows-visible D 槽 final-review package：`/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style` 目前 metadata/build 不新鮮（D checklist `[x]=32`, `[ ]=35`；D BUILD_ID `Nui2T0Kas-F4FyzGKYomC` != source clean build BUILD_ID `R9asswLYZcPv60FtAwhcX`）。
5. resubmit 前重跑並寫入 clean build/unit/api/e2e/live/browser/auth/AI preview/apply/rollback/export/PWA probes。

## 實測證據
- Active lock 已讀：`/home/sport/WORK/AGENTS/00_系統設定/ACTIVE_PRODUCT_LOCK.json` 指向本 task，mode=`PM_OP_QC_REPAIR_LOOP`。
- 固定文件已讀：`PRODUCT_SPEC.md`、`README.md`、`TEST_RESULT.md`、`FULL_BUILD_CHECKLIST.md`、`RC.md`、`TASK_META.json`。
- `node --run build`：exit_code=0；build 期間仍出現 `/api/calendar/google-fallback` 與 `/api/calendar/ics` dynamic server usage warning，但 build 成功。
- `node --run test:unit`：exit_code=0，19/19 PASS。
- `node --run test:api`：exit_code=0，116/116 PASS。
- `node --run test:e2e`：exit_code=0，11/11 PASS。
- Production `node --run start` 實啟；route table probes 全部 HTTP 200：`/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/billing`, `/audit`, `/more`, `/share-print`。
- Browser login 實測：`manager@shiftops.local / manager2026` 登入後進 `/calendar`；browser console 無 JS error；browser `fetch('/api/auth/me',{credentials:'include'})` 回 200。
- curl cookie auth：login HTTP 200 且回 `Set-Cookie: shiftops_session=...; Secure; HttpOnly; SameSite=lax`；同 cookie jar `/api/auth/me` HTTP 200。
- AI/API corrected live probe：preview HTTP 200 且 `previewToken` present；apply HTTP 200 且 `applyRunId`/`beforeSnapshot` present；rollback 使用 route/body applyRunId 後 HTTP 200。
- Export live probe：建立 ICS job 成功；status 宣告 `downloadUrl=/api/export/jobs/<jobId>/download`；download HTTP 200 `content-type: text/calendar` 且 body starts `BEGIN:VCALENDAR`。
- Blocking formal gate：source checklist raw counts `[x]=46`, `[ ]=21`；`TEST_RESULT.md`/`TASK_META.json` 誠實標示 not ready；因此即使正向 probes 通過仍不可 APPROVED。
- D package freshness：`/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style` 存在且有 executable contents，但 checklist/build metadata stale：D `[x]=32`, `[ ]=35`, D BUILD_ID `Nui2T0Kas-F4FyzGKYomC`, source BUILD_ID `R9asswLYZcPv60FtAwhcX`（本 source BUILD_ID 為本輪 QC clean build 後值）。

## 責任歸屬
OP_DELIVERY_DEFECT — 成品仍處於 OP returned_for_fix / not-ready gate，且 D 槽 final-review package 未同步最新 source truth/build metadata。

## 退回對象
OP / Sebastian。

## resubmit 條件
- `FULL_BUILD_CHECKLIST.md` 剩餘 21 項全部完成或正式逐項 scope-reconcile，並附 evidence。
- `TEST_RESULT.md`、`RC.md`、`NEXT_STEP.md`、`TASK_META.json` gate 欄位一致更新為 ready（僅在真完成後）。
- 修復/證明 audit persistence 與 D42 audit 缺口，不得以 API assertions PASS 但 persistence warning 覆蓋。
- 重新部署 D 槽待最終審核 package，確保 executable contents、truth-pack metadata、checklist counts、TEST_RESULT/RC/TASK_META/NEXT_STEP、BUILD_ID/manifests 與 source 最新狀態一致。
- 重送前提供 clean build/unit/api/e2e/live/browser/auth/export/PWA 證據。
