# Simon QC Verdict — REJECTED

product_id: 20260428_shift_scheduler_web_supershift_style
reviewed_at: 2026-04-30T11:45:18+08:00
verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP / Sebastian
active_lock: PM_OP_QC_REPAIR_LOOP respected; only active product reviewed.

## 必修項
1. OP 必須逐項完成或正式收斂 `FULL_BUILD_CHECKLIST.md` 剩餘 35 個未完成項，補可重現證據後才可重新送 build.ready/final review。
2. `TEST_RESULT.md` / `TASK_META.json` / `RC.md` / `NEXT_STEP.md` 必須同步為可送審狀態；目前正式 truth pack 自承 `all_must_fix_completed=false`、`ready_for_build_ready=false`。
3. D 槽 Windows-visible package 必須同步最新 truth-pack metadata；目前 D package `TEST_RESULT.md` 仍是 11:06 舊版（[x]=30/[ ]=37），source 已是 11:36（[x]=32/[ ]=35）。
4. API acceptance contract 需收斂：本輪 live `POST /api/ai/apply-preview` 回應有 `applyRunId`，但未回傳 `beforeSnapshot`，不符合 ACCEPTANCE Script M 對 RuleApplyRun/beforeSnapshot 的明確檢查。

## 實測證據
- 已讀 `ACTIVE_PRODUCT_LOCK.json`：active_product_id/path 符合本案，mode=`PM_OP_QC_REPAIR_LOOP`。
- 已讀 `PRODUCT_SPEC.md`、`README.md`、`TEST_RESULT.md`、`FULL_BUILD_CHECKLIST.md`、`TASK_META.json`。
- `TEST_RESULT.md`（source）標示：`all_must_fix_completed: false`、`ready_for_build_ready: false`，並列明 checklist `[x]=32`, `[ ]=35`。
- QC 獨立核對 `FULL_BUILD_CHECKLIST.md`：`[x]=32`、`[ ]=35`。未完成項包含 env/prisma/seed、holiday/lunar、coverage requirements、AI preference memory、彈性制度解析、技能覆蓋、calendar cell projection/差異/overlay/inspector/warnings、AI report、自然語言 export、AI conversation/action audit、rollback、README 完整、核心 spec 實作、migration、單一整合核查等。
- Clean verification：`node --run test:unit` → 19/19 PASS；`node --run test:api` → 83/83 PASS；`node --run test:e2e` → 11/11 PASS；`node --run build` → exit_code=0 PASS。
- Production `next start` route probes：PRODUCT_SPEC 指定 16 個頁面 route（`/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/billing`, `/audit`, `/more`, `/share-print`）均 HTTP 200。
- Browser probes：`/login`、`/calendar` 無 JS console error；`/calendar` 可見班別、缺口、加班 overlay controls。
- Auth cookie probe：login 200，回 `shiftops_session` Secure/HttpOnly/SameSite=Lax；cookie jar 呼叫 `/api/auth/me` → 200，email=`manager@shiftops.local`、role=`MANAGER`。
- AI flow probe：preview 回 `previewToken`、status=`partial`、calendarProjection length=31、warnings=4；apply 回 `applyRunId=run_1777520656143`，rollback 成功；但 apply response 未含 `beforeSnapshot`。
- Export probe：PDF/PNG/ICS job creation 保留 requested type；ICS endpoint HTTP 200 且內容以 `BEGIN:VCALENDAR` 開頭。
- D package check：`/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style` 存在且有 `package.json`、`.next`、app files；但 D `TEST_RESULT.md` 仍 generated_at `2026-04-30T11:06:30+0800`、`[x]=30/[ ]=37`，未同步 source 最新 11:36 truth-pack metadata。

## 判定
REJECTED / OP_DELIVERY_DEFECT。雖然本輪 build/test/live/browser/auth/export 多數正向探針通過，但正式交付 gate 自承未完成，checklist 仍有 35 項未完成，且 Windows-visible package metadata 未同步；不得 APPROVED，不得送 `build.ready`。

## resubmit 條件
1. 完成或正式收斂剩餘 35 個 checklist 項目，逐項提供實作/測試/route/browser/API 證據。
2. 修正/說明 `apply-preview` RuleApplyRun/beforeSnapshot contract，讓 ACCEPTANCE Script M 可照文件原樣重現，或正式更新規格與測試證據。
3. 重新跑 clean `node --run test:unit`、`node --run test:api`、`node --run test:e2e`、`node --run build`、production route probes、browser console probes、auth cookie、export/ICS probes。
4. 重新同步 D 槽待最終審核 package，包含最新 `TEST_RESULT.md`、`FULL_BUILD_CHECKLIST.md`、`RC.md`、`NEXT_STEP.md`、`TASK_META.json` 與 production build artifacts。
5. truth pack 必須一致顯示 `all_must_fix_completed=true`、`ready_for_build_ready=true` 或明確正式縮 scope 的核准依據；不得再自承 not ready 後送審。
