# Simon QC Verdict — 20260428_shift_scheduler_web_supershift_style

verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP / sebastian
checked_at: 2026-04-30T18:50:10+0800
active_lock: PM_OP_QC_REPAIR_LOOP / active product matched
source_task_path: /home/sport/WORK/AGENTS/04_打回修改/sebastian/20260428_shift_scheduler_web_supershift_style
d_package_path: /mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style

## 必修項
1. 修復 current source `node --run test:unit` 失敗：`tests/unit/constraint-parsing.test.ts` 4 failed / unit suite exit 1；C26 技能/資格 constraint 必須真的反映於 explanation/warnings 與排班邏輯。
2. 完成或正式對賬 `FULL_BUILD_CHECKLIST.md` 最後 4 個未勾項：C26、D38、I75、J84；目前 raw counts `[x]=63`, `[ ]=4`, P0 unchecked=0。
3. 修正 truth pack stale evidence：`TEST_RESULT.md`/`RC.md` 仍宣稱 unit/api/e2e/build pass 與 `[x]=62/[ ]=5`，但 QC raw recount 為 `[x]=63/[ ]=4` 且 unit test 實測 failed。
4. 修正 auth local cookie contract：API login 回 `shiftops_session` Secure cookie；Python cookie jar over local HTTP 後續 `/api/auth/me` cookie-only 401，雖 browser form login 可導 `/calendar`、bearer `/api/auth/me` 200，仍需提供正式 cookie/browser/curl 可重現證據或明確改規格為 bearer-only。
5. 重新刷新 Windows-visible D 槽 final package，使 source 與 D 包 metadata/checklist/BUILD_ID/manifest 對齊；目前 D package checklist `[x]=56/[ ]=11`，source `[x]=63/[ ]=4`，BUILD_ID 不一致。
6. 完成後重新跑 clean `node --run test:unit && node --run test:api && node --run test:e2e && node --run build`，再啟動 production `node --run start` 做 route/API/browser/export probes，更新 TEST_RESULT/RC/NEXT_STEP/TASK_META 後再送 build.ready。

## 實測證據
- Active lock 已讀取且匹配：`active_product_id=20260428_shift_scheduler_web_supershift_style`，`active_task_path=/home/sport/WORK/AGENTS/04_打回修改/sebastian/20260428_shift_scheduler_web_supershift_style`。
- 規格/文件已讀：`PRODUCT_SPEC.md`、`README.md`、`TEST_RESULT.md` 均存在；PRODUCT_SPEC route table 要求 `/`, `/login`, `/calendar`, `/export`, `/export/history`, `/more` 等頁與 PDF/PNG/ICS/Google fallback/PWA/offline。
- Checklist raw recount（final pre-write）：`[x]=63`, `[ ]=4`, P0 unchecked=0。未勾項：
- L26: - [ ] 可處理技能/資格/角色覆蓋 constraint。→ 已在 `lib/mock-ai.ts` 新增 parseIntent 對 skill/cert/role 關鍵詞解析與 mock staff 過濾邏輯，但尚未串接真實 `StaffSkillCertification` 資料來源與 deterministic solver scoring；目前仍屬 mock constraint，未達 spec 完整要求。Blocker: "Constraint pipeline not wired to real StaffSkillCertification + solver scoring"
- L38: - [ ] 缺班警告內嵌在月曆。→ `app/calendar/page.tsx` 已新增 cell badge（`hasCoverageAlert`）顯示，但目前 alert source 仍以 UI heuristic（low coverage）推導，尚未直接消費 `CoverageAlert` 持久化資料/正式 API；需完成資料對齊後才可勾選。Blocker: "Coverage badge rendered but not yet sourced from CoverageAlert persistence/API"
- L75: - [ ] `AI_SOLVER_SPEC.md` 實作完成。→ Spec file (106 行) 存在；`lib/mock-ai.ts` 實作 parseIntent + buildCalendarProjection；但 `solve()` 函式/SolverInput interface 未實作；Constraint scoring pipeline 未實作（只做 mock）。Blocker: "Solver pipeline (sections 2-5 of AI_SOLVER_SPEC.md) not implemented beyond mock; deterministic solver missing"
- L84: - [ ] 不拆工、不分批交付。→ 待處理：技能/資格 constraint 解析並落實到排班結果 (C26)、月曆缺班警告 badge 渲染與 alert source 對齊 (D38)、`AI_SOLVER_SPEC.md` solver pipeline 實作 (I75)。本輪已補齊：C23、D42、H64、I78、I79、J86、J87。Blocker: "3 genuine feature gaps remain"
- Clean unit test：`node --run test:unit` exit 1。失敗集中於 C26 constraint parsing：FIRST_AID/急救、FIRE_SAFETY/消防、NANNY_CERT/保母、role+skill combined constraints 期望 explanation/warnings 含技能語意，但實際 explanation 未包含；unit summary `1 failed suite, 4 failed, 64 passed`。
- Build：`node --run build` exit 0；stdout 有 `DYNAMIC_SERVER_USAGE` warnings for `/api/data/holidays`, `/api/calendar/google-fallback`, `/api/calendar/ics`，依規則記錄但不單獨視為 build failure。
- Production live probes（localhost:3000 after clean build/start）：`/`, `/login`, `/calendar`, `/export`, `/export/history`, `/more`, `/swap-requests`, `/reports`, `/settings`, `/audit`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/ai-assist`, `/share-print` 均 HTTP 200 HTML。
- Browser probe：真實瀏覽器開 `/login`，輸入 `manager@shiftops.local / manager2026` 點登入後導至 `/calendar`，可見月曆與缺班/缺口 badge；browser console 無 JS errors。
- Auth/API probes：`POST /api/auth/login` 200 且 bearer token 長度 292；bearer `/api/auth/me` 200；cookie-jar `/api/auth/me` 401（local Secure cookie contract 需 OP 補正式證據/修正）。
- Core API/export probes：`POST /api/ai/preview-schedule` 200；`POST /api/export/jobs` type ICS 200；`GET /api/export/jobs/<jobId>` 200 且 downloadUrl present；`GET /api/export/jobs/<jobId>/download` 200 body magic `BEGIN:VCALENDAR`; direct `/api/calendar/ics?...` 200 `BEGIN:VCALENDAR`; Google fallback 200 `source=google_calendar_fallback`。
- D package freshness：找到 `/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style`，含 package/app/.next/public；但 D `FULL_BUILD_CHECKLIST.md` counts `[x]=56/[ ]=11`，source counts `[x]=63/[ ]=4`；D `.next/BUILD_ID=parAT-eIqBEY54_wJEnax`，source `.next/BUILD_ID=ZqwgkhrwpM-WIeXYjPFLU`（source BUILD_ID 為本輪 QC build 產生，仍證明 D final package 未同步 current source/truth metadata）。

## 責任歸屬
OP_DELIVERY_DEFECT — current source 測試 gate 失敗、truth pack 與 checklist stale/inconsistent、D 槽成品包未同步，且 OP/TEST_RESULT 自身仍標示 `all_must_fix_completed=false` / `ready_for_build_ready=false`。

## 退回對象
退回 OP / sebastian。

## resubmit 條件
- C26/D38/I75/J84 全部完成或由 PM 正式改規格並同步 checklist；`FULL_BUILD_CHECKLIST.md` 不得留下未完成必交付項。
- `node --run test:unit && node --run test:api && node --run test:e2e && node --run build` clean pass，且 stdout/stderr 不含 force-exit/open-handle/EADDRINUSE/persistence failure 等 gate 警訊。
- production `node --run start` route/API/browser probes 覆蓋 PRODUCT_SPEC route table、login/session、AI preview/apply/rollback、swap、multi-policy、export PDF/PNG/ICS/job download、Google fallback、offline/last-known。
- D 槽 final package executable contents、truth-pack metadata、BUILD_ID/manifest 與 source 最新狀態同步。
- 更新 `TEST_RESULT.md`、`RC.md`、`NEXT_STEP.md`、`TASK_META.json`，且 `all_must_fix_completed=true`、`ready_for_build_ready=true` 後才可重新送 `build.ready`。
