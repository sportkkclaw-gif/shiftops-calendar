# Simon QC Verdict — REJECTED

- task_id: `20260428_shift_scheduler_web_supershift_style`
- reviewed_at: `20260430T131057+0800`
- active_lock: `PM_OP_QC_REPAIR_LOOP / 20260428_shift_scheduler_web_supershift_style`
- source_task_path: `/home/sport/WORK/AGENTS/04_打回修改/sebastian/20260428_shift_scheduler_web_supershift_style`
- d_package_path: `/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style`

## Verdict

REJECTED / OP_DELIVERY_DEFECT

## 必修項

1. 完成或逐項正式證據收斂 `FULL_BUILD_CHECKLIST.md` 剩餘 `35` 個未勾選項。
2. `TEST_RESULT.md` / `RC.md` / `TASK_META.json` 必須在證據完成後一致標示 `all_must_fix_completed=true` 與 `ready_for_build_ready=true`；不得在目前 formal gate=false 狀態送審。
3. 重新送審前再次同步 D 槽 final-review package，並保留 clean build/test/live/browser/API/export/D freshness 證據。

## 實測證據

### 1. 正式文件 / gate

- 已讀 `PRODUCT_SPEC.md`：第 19/20/21/29 節要求 test/build gate、ACCEPTANCE Script A–N、export download endpoint、D package freshness；PM 明確不縮 scope。
- 已讀 `README.md`：公開驗收入口要求 `node --run build && node --run start`，API 驗收包含 login / preview / apply / swap / fallback / ICS / export。
- 已讀最新 `TEST_RESULT.md`：`generated_at: 2026-04-30T13:48:00+0800`；line 8–9 仍為 `all_must_fix_completed: false`、`ready_for_build_ready: false`；line 36–39 明確寫 `FULL_BUILD_CHECKLIST.md` 仍 `[x]=32`, `[ ]=35`，remaining blocker 是收斂剩餘 35 checklist items。
- 獨立核對 `FULL_BUILD_CHECKLIST.md`：`[x]=32`, `[ ]=35`；未勾項包含 `.env.example`、Prisma migrations、demo seed、holiday/lunar、coverage requirement、AI preference memory、做四休四/2-2-3/DuPont/Pitman/Panama、split/on-call/compressed week、技能覆蓋、calendar cell/Day Inspector/alerts、AI reports、AI conversations/actions、rollback、README、核心規格實作、單一整合核查等。

### 2. Clean test/build gate

- `node --run test:api` after clearing ports 3000/3101：exit_code=0；Test Suites `6 passed`; Tests `90 passed, 90 total`。
- `node --run build`：exit_code=0，production build PASS（Next dynamic server usage messages for `/api/calendar/ics` and `/api/calendar/google-fallback` appeared during static generation but build completed successfully）。

### 3. Live / browser probes

- Production `node --run start` running on localhost:3000。
- Route probes：`/login`, `/calendar`, `/export`, `/export/history`, `/api/calendar/ics?locationId=loc_demo&start=2026-05-01&end=2026-05-31` all HTTP 200。
- Browser login probe：opened `/login`, filled `manager@shiftops.local / manager2026`, clicked login, landed on `/calendar`; browser console/js_errors = 0。
- Script-M style preview and export download were reported fixed in latest `TEST_RESULT.md`; prior live probe in this QC round observed preview token and ICS download HTTP 200. Current clean API test also covers export download route-handler cases.

### 4. D 槽 package

- D final-review package exists and contains executable product files (`package.json`, `app/`, `.next/`, `public/`, tests, specs, truth pack)。
- Latest source vs D metadata is currently synchronized for `TEST_RESULT.md`, `RC.md`, `TASK_META.json`, and `FULL_BUILD_CHECKLIST.md` at OP sync time.
- Current source `.next/BUILD_ID` differs from D `.next/BUILD_ID` because QC reran `node --run build` after OP sync; this QC-induced BUILD_ID drift is not used as the rejection reason.

## 判斷

雖然本輪 API/build/live/browser probes 多數已通過，正式 truth pack 仍誠實標示 not ready，且 `FULL_BUILD_CHECKLIST.md` 仍有 `35` 個 release evidence gate 未完成/未收斂。依 quality-verdict 規則：OP/TEST_RESULT/RC 已標示 `all_must_fix_completed=false` / `ready_for_build_ready=false` 時，即使 clean probes 通過也不得 APPROVED。

## 責任歸屬

OP_DELIVERY_DEFECT

## 退回對象

退回 OP / Sebastian。

## Resubmit 條件

1. 完成或正式證據收斂 `FULL_BUILD_CHECKLIST.md` 剩餘 `35` 項，且不得縮 scope 除非 PM 正式變更規格。
2. 更新 `TEST_RESULT.md` / `RC.md` / `TASK_META.json`，使 `all_must_fix_completed=true`、`ready_for_build_ready=true` 與 checklist/evidence 一致。
3. 重新同步 D 槽 final-review package，提供 metadata + BUILD_ID/manifest freshness evidence。
4. 重新跑 clean `node --run build`、`node --run test:unit`、`node --run test:api`、`node --run test:e2e`、production route probes、browser login/calendar hydration、auth cookie、Script M、export download probes，全部附證據後再送 `build.ready`。
