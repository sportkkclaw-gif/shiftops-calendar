# Simon QC Verdict — 20260430T112451+0800

verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP/sebastian
active_product_id: 20260428_shift_scheduler_web_supershift_style
active_task_path: /home/sport/WORK/AGENTS/04_打回修改/sebastian/20260428_shift_scheduler_web_supershift_style

## 必修項
1. 完成或正式收斂 `FULL_BUILD_CHECKLIST.md` 仍未勾選的 37 項，逐項補實作與可驗收證據；在此之前不得送 `build.ready`。
2. 重新同步 truth pack，使 `RC.md`、`NEXT_STEP.md`、`TASK_META.json`、`TEST_RESULT.md` 與 checklist gate 一致，不可一邊宣稱 ready、一邊保留大量未完成項。
3. 若重新 resubmit，需以乾淨環境重跑 `node --run test:unit`、`node --run test:api`、`node --run test:e2e`、`node --run build`、production `next start` route/API/browser probes，並重新同步 D 槽 Windows-visible package。

## 實測證據
- ACTIVE_PRODUCT_LOCK 已核對：mode=`PM_OP_QC_REPAIR_LOOP`，active task path 符合本產品。
- 已讀 `PRODUCT_SPEC.md`、`README.md`、`TEST_RESULT.md`、`FULL_BUILD_CHECKLIST.md`、`RC.md`、`TASK_META.json`。
- `TEST_RESULT.md` 明確標示：`all_must_fix_completed=false`、`ready_for_build_ready=false`，結論為 not ready for build.ready。
- `FULL_BUILD_CHECKLIST.md` 實際仍為 `[x]=30`, `[ ]=37`；未完成項分布於 env/migrations/seed、holiday/lunar、AI solver 多制度語意、calendar cell/OT/inspector、AI reports、audit/rollback、P0 spec implementation、整合核查等。
- QC 乾淨重跑：`node --run test:unit` exit 0（19/19 PASS）、`node --run test:api` exit 0（83/83 PASS）、`node --run test:e2e` exit 0（11/11 PASS）、`node --run build` exit 0。
- Production `next start` 啟動後，PRODUCT_SPEC 頁面 route probe 全部 HTTP 200：`/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/billing`, `/audit`, `/more`, `/share-print`。
- Browser `/login` 與 `/calendar` 實開無 JS error；`/calendar` 顯示 2026 年 04 月月曆、班別、缺口、加班 overlay 與設定控制。
- Auth live probe：`POST /api/auth/login` 200，回 `Set-Cookie: shiftops_session=...; HttpOnly; Secure; SameSite=lax`；cookie jar 呼叫 `/api/auth/me` 200。
- Export live probe：PDF/PNG/ICS job create/query 均保持 requested type；ICS endpoint 200，回傳 `BEGIN:VCALENDAR`。
- D 槽 package 存在且含 `package.json`、`app/`、`.next/`、truth-pack metadata；D package BUILD_ID 仍為 TEST_RESULT 宣稱的 `btAhVvbXnqwmQCrej6uSN`。本輪 QC 重新 build 後 source `.next/BUILD_ID` 變為 `2x6GRTbNIQIO7T7OYM_g5`，未將此 QC build 視為 OP package freshness 失敗主因。

## 判定
雖然 build/test/live/browser/API 正向探針目前多數通過，但正式 truth pack 自承 `all_must_fix_completed=false` / `ready_for_build_ready=false`，且 `FULL_BUILD_CHECKLIST.md` 仍有 37 項未完成。依 quality-verdict gate，產品不可 final approve。

## resubmit 條件
- OP 完成或正式收斂 37 項 checklist，逐項留下證據。
- truth pack 四件套同步一致：`RC.md`、`NEXT_STEP.md`、`TASK_META.json`、`TEST_RESULT.md`。
- 重新提供 clean build/test/API/live/browser evidence 與 D 槽 Windows-visible package freshness 證據。
