# Simon QC Verdict — REJECTED

checked_at: 2026-04-30T21:08:27+08:00
verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP / Sebastian
active_product_id: 20260428_shift_scheduler_web_supershift_style
active_task_path: /home/sport/WORK/AGENTS/04_打回修改/sebastian/20260428_shift_scheduler_web_supershift_style

## 必修項
1. 重新產出並同步 Windows-visible final-review package 到 `D:\WORK\成品區\待最終審核\sebastian\20260428_shift_scheduler_web_supershift_style`；目前該正式待審路徑不存在/為空，不可用 `_退回修改` package 取代 final-review 入口。
2. 以目前 source 重新打包，使 D package `.next/BUILD_ID` / manifests / `TASK_META.json` / `RC.md` / `NEXT_STEP.md` / `TEST_RESULT.md` / `FULL_BUILD_CHECKLIST.md` 與 source 一致。
3. 清除 truth-pack 矛盾：目前 source 同時有多筆 `APPROVED` 歷史欄位，但正式 `TASK_META.final_review_status=returned_for_fix` / `release_status=not_publishable_returned_for_fix`，且 D package 被移到 `_退回修改`；重新送審前需只保留一致的 build.ready 待審狀態與 fresh package evidence。
4. 重新跑並記錄 clean build/unit/api/e2e、production route table、browser login/cookie session、AI preview/apply/rollback、calendar/report API、PDF/PNG/ICS export download 與 D package freshness probes。

## 實測證據
- Active lock 已讀取並符合 `PM_OP_QC_REPAIR_LOOP`：僅處理 active product `20260428_shift_scheduler_web_supershift_style`。
- 文件已讀：`PRODUCT_SPEC.md`、`README.md`、`TEST_RESULT.md`、`FULL_BUILD_CHECKLIST.md`、`TASK_META.json`。
- Source checklist raw count：`[x]=67`, `[ ]=0`, P0 unchecked=0。
- 技術 gate 本輪 pass：delegate/本輪驗證回報 `node --run test:unit` 98/98 PASS、`node --run test:api` 159/159 PASS、`node --run test:e2e` 11/11 PASS、`node --run build` exit 0（dynamic route warnings only）。
- Production live probes：`node --run start` on `http://localhost:3000` Ready；PRODUCT_SPEC route table `/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/billing`, `/audit`, `/more`, `/share-print` 全部 HTTP 200。
- Browser login：實際填 `manager@shiftops.local / manager2026` 後導入 `/calendar`，browser console/js_errors empty。
- Auth cookie：curl cookie jar `POST /api/auth/login` 後 `GET /api/auth/me` = 200；Python `http.cookiejar` 對 local Secure cookie 出現 401，已依 QC 規則以 curl/browser 補證，不列產品缺陷。
- Core API probes：AI preview/apply/rollback = 200/200/200；calendar projection/day-inspector/coverage-summary/google fallback/last-known = 200；PDF/PNG/ICS export job download = 200，MIME/magic bytes 分別 `%PDF`、PNG、`BEGIN:VCALENDAR`；direct ICS = 200 `BEGIN:VCALENDAR`。
- D 槽正式待審入口：`/mnt/d/WORK/成品區/待最終審核/sebastian` 存在但無 `20260428_shift_scheduler_web_supershift_style` package。
- D 槽實際找到的 package 位於 returned lane：`/mnt/d/WORK/成品區/_退回修改/sebastian/20260428_shift_scheduler_web_supershift_style`，不是 `待最終審核`。
- BUILD_ID freshness：source `.next/BUILD_ID` = `mP8VjenOXfNd8hSRw7fsP`；`_退回修改` D package `.next/BUILD_ID` = `7o11N6N3VzyhPH4Uhgxg7`，不一致。此 mismatch 是本輪 clean build 後觀測到的 source/D freshness gap；正式 `待最終審核` package 仍缺失。
- Truth metadata freshness：本輪寫入 verdict 前觀測到 source vs `_退回修改` D `TASK_META.json` 不一致；verdict 寫入後已將 truth-pack metadata 同步到 `_退回修改` package（current source/D TASK_META sha `a3042c3dbc295c3a40dbbf8490b3e108e28bffe82437c2bf61b5e0c72ad2ebfa` / `a3042c3dbc295c3a40dbbf8490b3e108e28bffe82437c2bf61b5e0c72ad2ebfa`）。但正式待審 package仍不存在，且 `_退回修改` BUILD_ID 與 current source 不一致，因此 release gate 仍失敗。

## 責任歸屬
OP_DELIVERY_DEFECT — source 技術功能可跑，但 OP 未提供與目前 source 對齊且位於正式 `D:\WORK\成品區\待最終審核` 的 Windows-visible final-review package，truth-pack/發佈狀態亦互相矛盾，不能放行。

## 退回對象
退回 OP / Sebastian。

## resubmit 條件
1. 將 fresh executable product package 部署到 `D:\WORK\成品區\待最終審核\sebastian\20260428_shift_scheduler_web_supershift_style`，不得只放 `_退回修改`。
2. D package 與 source 的 `.next/BUILD_ID`、route manifests、`FULL_BUILD_CHECKLIST.md`、`TEST_RESULT.md`、`RC.md`、`NEXT_STEP.md`、`TASK_META.json` hash/mtime/關鍵 gate 欄位一致。
3. Truth pack 狀態一致：重新送審時 `next_agent=simon` / `next_event=build.ready`（或 dispatcher 成功後 next_event=null），不得同時宣稱 approved 與 returned/not_publishable。
4. 附上 clean build/unit/api/e2e、live route/browser/auth、AI preview/apply/rollback、calendar/report、PDF/PNG/ICS export download、D package freshness evidence 後重新送審。
