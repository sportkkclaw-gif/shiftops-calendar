# Simon QC verdict — 20260430T163401+0800

verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP / sebastian
active_product_lock: PM_OP_QC_REPAIR_LOOP / 20260428_shift_scheduler_web_supershift_style
status: returned_for_fix
next_event: null

## 必修項
1. 修復 production build 失敗：`app/api/ai/coverage-summary/route.ts` 目前存在重複宣告 `shiftTypes` 與 `shiftTypeMap`，`node --run build` exit_code=1。
2. 修復可重現測試 gate：`node --run test:api` exit_code=1（server 未在 30000ms ready，135/135 failed）；`node --run test:e2e` exit_code=1（找不到可用 production build / `.next` BUILD_ID，11/11 failed，且 Jest open handle warning）。
3. 仍需完成或正式 evidence/reconcile `FULL_BUILD_CHECKLIST.md` 剩餘未勾項：source raw count `[x]=46`, `[ ]=21`, P0 unchecked=0。
4. 修復後重新 clean build/test/live route/browser/auth/export probes，並同步 Windows-visible D package。

## 實測證據
- ACTIVE_PRODUCT_LOCK 已讀取：active_task_path 指向本 task；本輪只處理 active product。
- `node --run build`（/tmp/shiftops_qc symlink to formal path）→ exit_code=1。
  - 首次 build 在 OP 併發修改前/中出現 TypeScript error：`include` not assignable to `never` at `app/api/ai/coverage-summary/route.ts`.
  - 重讀檔案後乾淨重跑 build 仍失敗：webpack compile error `the name shiftTypes is defined multiple times` at lines 78 and 95；`shiftTypeMap` duplicated at lines 81 and 96.
- `node --run test:unit` → exit_code=0，2 suites / 19 tests PASS。
- `node --run test:api` → exit_code=1，8 suites failed，135 tests failed；共同錯誤為 `Server at http://localhost:3101/ did not become ready within 30000ms`。
- `node --run test:e2e` → exit_code=1，11 tests failed；server output: `Could not find a production build in the '.next' directory`，hook timeout，並出現 `Jest did not exit one second after the test run has completed`。
- Production live/browser probes 本輪未成立：clean `node --run build` 已失敗，不能以舊/殘留 `.next` 或 D package 覆蓋 build gate。
- D package inspection: `/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style` exists and contains executable Next.js contents; D checklist `[x]=46`, `[ ]=21`. D `.next/BUILD_ID`=`D91_FkNzIZJ2l2IDqrhxc`; source `.next` was invalidated by failed clean build, so D package is not evidence of current source build passing.

## 責任歸屬
OP_DELIVERY_DEFECT — 新增/修改的 route code 導致 clean production build 與 API/E2E tests 不可重現；ready flags 原本仍 false，且 checklist 尚未完成。

## 退回對象
OP / sebastian

## resubmit 條件
1. `node --run build` clean exit_code=0，無 duplicate declaration / Prisma type compile error。
2. `node --run test:unit`、`node --run test:api`、`node --run test:e2e` clean exit_code=0；不得有 server lifecycle/open-handle/EADDRINUSE/timeout 警訊。
3. 啟動 production `node --run start` 後，對 PRODUCT_SPEC route table、login cookie auth、AI preview/apply/rollback、export job/download、ICS/Google fallback 做 live probes 並記錄證據。
4. 完成或正式 evidence/reconcile checklist 剩餘 `[ ]=21` 項，將 `all_must_fix_completed` / `ready_for_build_ready` 與實際 gate 同步。
5. 重新同步 D 槽待最終審核 package（含 source、truth pack、`.next/BUILD_ID`/manifest），再送審。
