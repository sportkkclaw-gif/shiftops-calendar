# Simon QC Verdict — REJECTED

product_id: 20260428_shift_scheduler_web_supershift_style
active_lock: PM_OP_QC_REPAIR_LOOP
reviewed_at: 2026-04-30T10:44:04+08:00
reviewer: Simon / 蘇衡
verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP / Sebastian

## 必修項
1. 重新部署 Windows-visible D 槽待最終審核 package，必須與正式 source 本輪 build/truth pack 同步；至少 `.next/BUILD_ID`、`TEST_RESULT.md`、`FULL_BUILD_CHECKLIST.md` 不可停留舊版。
2. 補齊或同步 `FULL_BUILD_CHECKLIST.md` 的完整 acceptance evidence；目前 truth pack 宣稱 `all_must_fix_completed=true` / `ready_for_build_ready=true`，但 checklist 仍大量未勾，屬假完成風險。
3. 修正 README / ACCEPTANCE curl 合約與實際 API contract 不一致：rollback script 只送 `reason` 會 400；export script 未送 `organizationId/locationId` 會 400。若正式 contract 必須需要這些欄位，文件與 TEST_RESULT 必須同步；若文件為準，API 必須兼容。
4. 重送正式 `build.ready` 前，需以乾淨環境重跑 build/test/live probes，並同步 D 槽 package 與 truth pack。

## 實測證據
- Active lock 已讀取：`/home/sport/WORK/AGENTS/00_系統設定/ACTIVE_PRODUCT_LOCK.json` 指向本產品，本輪未處理其他 task。
- Source verification（/home/sport/WORK/AGENTS/04_打回修改/...）：
  - `node --run test:api` → exit 0，83/83 PASS。
  - `node --run test:unit` → exit 0，19/19 PASS。
  - `node --run test:e2e` → exit 0，11/11 PASS，embedded Next server clean stop。
  - `node --run build` → exit 0；Next dynamic-server-usage diagnostics appeared for calendar fallback/ics during static collection but build completed.
- Production live route probes on clean `node --run start` localhost:3000:
  - `/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/billing`, `/audit`, `/more`, `/share-print` → all HTTP 200.
  - Browser opened `/calendar`; calendar grid, expected shifts, assigned staff, gap/conflict badges, overtime overlay controls visible.
  - `POST /api/auth/login` manager demo → 200 and `Set-Cookie: shiftops_session=...; HttpOnly; SameSite=lax`; cookie jar contains `shiftops_session`; `/api/auth/me` with cookie → 200.
  - AI preview → 200, `calendarProjection=31`, `proposedAssignments=40`, overtime candidates present; apply → 200; corrected rollback body → 200.
  - Swap request create → 201; manager approve → 200; multi-policy → 200.
  - Correct export body with `organizationId/locationId`: PDF/PNG/ICS job create → 200, type persisted.
  - ICS endpoint → 200 `text/calendar`, begins `BEGIN:VCALENDAR`.
- Blocking defects:
  - D package stale: source `.next/BUILD_ID=R1cqtVQMhSmcuwtZf0MWr` mtime 2026-04-30 10:41:41, D package `.next/BUILD_ID=bshXgTShtPPDYAUTMgF89` mtime 2026-04-30 10:13:38.
  - D package truth pack stale: source `TEST_RESULT.md` mtime 2026-04-30 10:38:20 size 1387; D package `TEST_RESULT.md` mtime 2026-04-30 10:03:23 size 4669. Source `FULL_BUILD_CHECKLIST.md` mtime 2026-04-30 10:37:36; D package `FULL_BUILD_CHECKLIST.md` mtime 2026-04-28 18:24:00.
  - `FULL_BUILD_CHECKLIST.md` in source still has `[x]=30`, `[ ]=37`; numerous acceptance/P0 items remain unchecked while `TEST_RESULT.md`/`TASK_META.json` claim all must-fix complete and ready for build.ready.
  - README/ACCEPTANCE documented rollback probe (`POST /api/ai/apply-runs/<id>/rollback` with only reason) reproduced 400: fields `applyRunId` and `organizationId` required.
  - README documented export probe without `organizationId/locationId` reproduced 400: fields `organizationId` and `locationId` required.

## Verdict
REJECTED — Source build/tests/live probes are improved and many core flows pass, but Windows-visible final review package is stale, truth-pack checklist/gate evidence remains contradictory, and documented acceptance scripts are not reproducible as written.

## Resubmit 條件
- OP/Sebastian redeploys the latest source build and truth pack into `D:\WORK\成品區\待最終審核\sebastian\20260428_shift_scheduler_web_supershift_style`.
- OP/Sebastian reconciles checklist evidence: either complete/check the applicable acceptance/P0 items with evidence or remove invalid `all_must_fix_completed/ready_for_build_ready` claims.
- OP/Sebastian updates README/ACCEPTANCE/TEST_RESULT or API compatibility so the published one-command curl probes for rollback/export run cleanly.
- OP/Sebastian reruns and records: `node --run test:api`, `node --run test:unit`, `node --run test:e2e`, `node --run build`, production route probes, auth cookie probe, export probe, D package freshness comparison; then resubmits `build.ready`.
