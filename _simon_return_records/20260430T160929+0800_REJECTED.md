# Simon QC Verdict — REJECTED

- task_id: `20260428_shift_scheduler_web_supershift_style`
- task_path: `/home/sport/WORK/AGENTS/04_打回修改/sebastian/20260428_shift_scheduler_web_supershift_style`
- generated_at: 2026-04-30T16:09:29.067570+08:00
- verdict: **REJECTED**
- responsibility: **OP_DELIVERY_DEFECT**
- return_to: **OP / sebastian**
- next_event: `null` (no `review.done`, no `build.ready`)

## Verdict basis
Product remains formally not releasable because the official truth pack still declares `all_must_fix_completed=false` and `ready_for_build_ready=false`, and raw `FULL_BUILD_CHECKLIST.md` still has `[x]=46`, `[ ]=21`. This is not a PM ambiguity; it is an OP delivery gate not completed/reconciled.

## Required fixes / resubmit conditions
1. Complete or formally reconcile every remaining `FULL_BUILD_CHECKLIST.md` unchecked item; current raw count is `[x]=46`, `[ ]=21`.
2. Update `TEST_RESULT.md`, `RC.md`, `NEXT_STEP.md`, and `TASK_META.json` so `all_must_fix_completed` / `ready_for_build_ready` only become true after the checklist is fully evidenced.
3. Preserve the passing build/test/live/browser evidence after the checklist reconciliation; rerun build/test in a clean environment before resubmitting.
4. Keep the Windows-visible final review package under `D:\WORK\成品區\待最終審核\sebastian\20260428_shift_scheduler_web_supershift_style` synchronized with source metadata and product contents.

## Live verification evidence from this QC round
- `node --run build` from clean source tree: exit_code=0, PASS. Next.js emitted `DYNAMIC_SERVER_USAGE` warnings for `/api/calendar/google-fallback` and `/api/calendar/ics`, but build completed successfully.
- `node --run test:unit`: exit_code=0, 19/19 PASS.
- `node --run test:api`: exit_code=0, 116/116 PASS.
- `node --run test:e2e`: exit_code=0, 11/11 PASS.
- Production `node --run start` on `http://localhost:3000`: Ready.
- Spec route probes all returned HTTP 200: `/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/billing`, `/audit`, `/more`, `/share-print`.
- Browser login form: `manager@shiftops.local / manager2026` submitted successfully and navigated to `/calendar`; console contained no JS errors after login.
- Cookie auth probe: `POST /api/auth/login` returned 200 with `set-cookie: shiftops_session=...`; same cookie jar `GET /api/auth/me` returned 200 with MANAGER user.
- API/export probes: preview schedule returned 200 with `previewToken`; PDF/PNG/ICS export job creation returned 200/PENDING; direct ICS endpoint returned 200 `text/calendar`; Google fallback returned 200 JSON.

## D package evidence
- D package found at `/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style` with executable contents (`package.json`, `app/`, `public/`, `.next/BUILD_ID`).
- D checklist metadata currently reads `[x]=46`, `[ ]=21`.
- Source BUILD_ID after QC clean build: `MyoKzDJ9Sp1AlmwwLeX9W`. D BUILD_ID: `D91_FkNzIZJ2l2IDqrhxc`. BUILD_ID difference was introduced by this QC clean build after OP's package sync, so it is recorded as QC-induced drift, not the primary rejection reason.

## Remaining checklist blockers (raw)
1. Holiday/Lunar seed 顯示。→ `schema.prisma` 無 Holiday/LunarDate model；`seed.ts` 無相關 seed。Blocker: "Holiday/LunarDate model absent from schema.prisma"
2. AI preference memory 可讀寫。→ 無 `AiPreference`/`UserAiPreference` model；`schema.prisma` 無相關 model。Blocker: "No AI preference model in schema.prisma"
3. 命名制度只作 seed，可調整 phase/天數/班別/組別 offset。→ `NamedPatternSeed` model 存在 (`schema.prisma:253-259`) 但無 UI/API 調整機制；`seed.ts` 只建立 seed。Blocker: "No API/UI to adjust named pattern phase/days/shift/group offset"
4. 可處理技能/資格/角色覆蓋 constraint。→ `parseIntent` 無技能/資格解析；`buildCalendarProjection` 不考慮 `StaffSkillCertification`；Mock AI 固定 staff assignment。Blocker: "parseIntent skill/certification constraint parsing not implemented"
5. 月曆 cell 可直接顯示制度應排班別（A/B/夜/休/待命/兩段班）。→ Calendar UI (page.tsx) 存在但 cell display logic 僅在 spec 文件；`CalendarDayProjection` model (`schema.prisma:156-169`) 存在但 `/calendar` API route 不存在；`app/api/calendar/` 無 projection endpoint。Blocker: "No /api/calendar/proj
6. 月曆 cell 可顯示實際已排人員與制度應排班別差異。→ 同上，無 projection API consumer。Blocker: "No calendar projection API;差异显示 only in spec docs"
7. 加班 overlay 可切換：隱藏 / 徽章 / 候選人 / 已確認。→ `CalendarCellDisplaySetting.overtimeDisplayMode` 存在 (`schema.prisma:80`)；`app/calendar/page.tsx` 有 OT toggle UI；但無后端 API 持久化偏好。Blocker: "No PATCH /api/settings/calendar-display API; UI toggle exists but preference not
8. Day Inspector 可看當日人員與 AI 警告解釋。→ `app/calendar/page.tsx` UI 存在但無 `/api/calendar/day-inspector` 或類似 API。Blocker: "Day Inspector UI stub present; no dedicated API route"
9. 缺班警告內嵌在月曆。→ `CoverageAlert` model 存在；Mock AI 產生 `coverageAlerts` (lines 125-135)；但月曆 UI 無 badge 渲染 logic。Blocker: "CoverageAlert model+seed exists; badge rendering in calendar UI not implemented"
10. 超時風險內嵌在月曆或 Inspector。→ `OvertimeCandidate` 含 `riskFlags` (JSON string)；但月曆 UI 無 OT risk display。Blocker: "OT riskFlags in model; UI rendering not implemented"
11. AI 可回答「本月缺口在哪」。→ `/api/ai/preview-schedule` + mock AI 可產生 `coverageAlerts`；但無獨立 `/api/ai/coverage-summary` route。Blocker: "No dedicated /api/ai/coverage-summary route;缺口 answer only via preview flow"
12. 加班 assignment 不覆蓋原班別並寫入 audit。→ Mock AI `buildCalendarProjection` 僅顯示 OT candidates 不取代 OFF shifts；但 `/api/ai/apply-preview` 未持久化 beforeSnapshotRef 且未寫入 AI action AuditEvent。Blocker: "OT assignment audit persistence missing in apply-preview flow"
13. 回答可附 heatmap / bar chart / table。→ UI (`/app/reports/page.tsx`) 存在但無對應 API。Blocker: "Reports page UI stub; no analytics API endpoints"
14. AI conversations 留存。→ 無 `AICONversation` model；`AuditEvent` model 存在但只用於 swap/apply 事件。Blocker: "No AICONversation model in schema.prisma"
15. `AI_SOLVER_SPEC.md` 實作完成。→ Spec file (106 行) 存在；`lib/mock-ai.ts` 實作 parseIntent + buildCalendarProjection；但 `solve()` 函式/SolverInput interface 未實作；Constraint scoring pipeline 未實作（只做 mock）。Blocker: "Solver pipeline (sections 2-5 of AI_SOLVER_SPEC.md) not 
16. `CALENDAR_PROJECTION_ALGORITHM.md` 實作完成。→ Spec file (82 行) 存在；`CalendarDayProjection` model 存在 (`schema.prisma:156-169`)；但 `cycleDay()` / `phaseLabel()` / multi-policy overlay functions 未實作；無 `/api/calendar/projection` route。Blocker: "cycleDay/phaseLabel
17. `RBAC_MATRIX.md` 權限測試通過。→ Spec file (48 行) 存在；`lib/rbac.ts` 實作基礎 RBAC；但 matrix 測試 (section 4) 無獨立測試檔；member AI scope / manager cross-location blocking 未驗證。Blocker: "No standalone RBAC matrix test suite; lib/rbac.ts basic logic exists but full matrix not 
18. `PRISMA_SCHEMA_SPEC.md` 對應 migration 完成。→ Spec file (336 行) 存在；`schema.prisma` 包含所有 spec models；但無 migrations folder (使用 `db push`)；需確認 `prisma migrate deploy` 可達到同樣 state。Blocker: "No migrations/ folder; db push used instead of migrate; production deplo
19. 不拆工、不分批交付。→ 当前状态: AI solver 未完成 (C21, C22, C23, C26), 日曆 projection 未完成 (D34-D41), AI 報告未完成 (F51-F53), Audit/AI conversation 未完成 (H64-H65), Spec implementations 掛起 (I75-I79)。仍有多個子系統未交付。Blocker: "Multiple subsystems pending: AI solver pipeline, calendar p
20. Calendar UI 直接讀 projection，不自行推算班別。→ Calendar page (`/app/calendar/page.tsx`) 存在但無 `/api/calendar/projection` route 讀取；目前直接呼叫 `/api/ai/preview-schedule` 當 projection source。Blocker: "Calendar page reads from /api/ai/preview-schedule, not a dedicated cale
21. SolverPreview 同時供 AI、calendar、apply、audit 使用。→ `SolverPreview` type 存在 (`lib/mock-ai.ts`)；但不同 consumer 之間未建立共享 contract；AI preview 結果不自動同步到 calendar projection。Blocker: "SolverPreview consumed by apply-preview and rollback; calendar projection has separa

## Conclusion
REJECTED / OP_DELIVERY_DEFECT. Return to OP (sebastian). Resubmit only after the formal checklist/gate is complete or explicitly reconciled with evidence and the D package is refreshed accordingly.
