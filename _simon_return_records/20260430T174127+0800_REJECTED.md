# Simon QC Verdict — REJECTED

generated_at: 2026-04-30T17:41:27+08:00
generated_by: simon-qc-cron
active_product_id: 20260428_shift_scheduler_web_supershift_style
active_task_path: /home/sport/WORK/AGENTS/04_打回修改/sebastian/20260428_shift_scheduler_web_supershift_style
verdict: REJECTED
responsibility: OP_DELIVERY_DEFECT
return_to: OP / sebastian
next_event: null

## 必修項
1. 完成或正式證據化/收斂 `FULL_BUILD_CHECKLIST.md` 仍未勾選的 19 項；目前 raw count: `[x]=48`, `[ ]=19`, P0 unchecked=0。
2. 將 `all_must_fix_completed` 與 `ready_for_build_ready` 只在真實完成後改為 true；目前 TASK_META 仍為 `all_must_fix_completed=False`, `ready_for_build_ready=False`。
3. 重新同步 Windows-visible final-review package：`/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style`。目前 D package 存在=True，但 metadata stale=['TEST_RESULT.md', 'RC.md', 'NEXT_STEP.md', 'TASK_META.json', 'FULL_BUILD_CHECKLIST.md']；D BUILD_ID=`D91_FkNzIZJ2l2IDqrhxc`，本輪 QC 乾淨重建後 source BUILD_ID=`4LHbMQtp4c2DUl_SRNkUt`（此 BUILD_ID drift 含 QC rebuild 影響，單獨不當缺陷；metadata stale/ready flags/checklist 未完成仍為阻擋）。
4. resubmit 前需重跑並記錄 clean build/unit/api/e2e/live/browser/auth/export probes，然後才可送 `build.ready`。

## 實測證據
- ACTIVE_PRODUCT_LOCK 已遵守：只處理 active product `20260428_shift_scheduler_web_supershift_style`；未驗收其他 task。
- `node --run build`：exit_code=0 PASS；Next.js compiled 16 PRODUCT_SPEC page routes and API routes。Build stdout 仍有 `/api/calendar/google-fallback`、`/api/calendar/ics`、`/api/data/holidays` 的 `DYNAMIC_SERVER_USAGE` warning；因 build 成功且 live probes 通過，記錄為 warning，不列 build failure。
- `node --run test:unit`：exit_code=0，2 suites / 19 tests PASS。
- `node --run test:api`：exit_code=0，9 suites / 150 tests PASS；stdout 含 holidays/preferences 測試故意模擬 DB error 的 console.error，但 assertions PASS。
- `node --run test:e2e`：exit_code=0，1 suite / 11 tests PASS；embedded Next server start/stop 正常。
- Production `node --run start` live probes：`/`, `/login`, `/calendar`, `/ai-assist`, `/data/staff`, `/data/shifts`, `/data/holidays`, `/swap-requests`, `/reports`, `/export`, `/export/history`, `/settings`, `/billing`, `/audit`, `/more`, `/share-print` 全部 HTTP 200。
- Browser login form：`/login` 使用 `manager@shiftops.local / manager2026` 登入後導向 `/calendar`；browser credentialed `fetch('/api/auth/me',{credentials:'include'})` 回 200 `application/json`。
- curl cookie auth：`POST /api/auth/login` HTTP 200，cookie jar 收到 HttpOnly `shiftops_session`；同 cookie `GET /api/auth/me` HTTP 200，role=MANAGER。
- Protected/live API smoke：calendar projection、day inspector、google fallback、last-known 全 HTTP 200；ICS endpoint HTTP 200 `text/calendar` 且內容 `BEGIN:VCALENDAR`。
- Export job smoke：建立 ICS job HTTP 200，status advertised download URL；`GET /api/export/jobs/<jobId>/download` HTTP 200 `text/calendar` 且內容 `BEGIN:VCALENDAR`。

## 判斷假完成風險
- 本輪技術 probes 綠燈，但正式 truth pack 誠實標示 not ready：`all_must_fix_completed=false`、`ready_for_build_ready=false`、`next_event=null`。
- Checklist 仍有 19 項未勾；依 quality-verdict 補充規則，即使 build/test/live 通過，只要 OP 交付 gate 未達，不可 APPROVED。
- D 槽 package 可執行內容存在，但 truth-pack metadata 與 source 不同步，Windows-visible final-review package 尚未達 resubmit gate。

## 責任歸屬
OP_DELIVERY_DEFECT — OP 尚未完成/收斂 checklist、ready flags 與 D final-review package synchronization；PM 規格已存在且可驗收，未歸 PM_SPEC_DEFECT。

## 退回對象
退回 OP / sebastian。

## resubmit 條件
1. `FULL_BUILD_CHECKLIST.md` 剩餘 19 項完成或正式 scope reconciliation，有逐項證據且 raw counts/flags 同步。
2. `TASK_META.json` / `RC.md` / `NEXT_STEP.md` / `TEST_RESULT.md` truthfully show ready only after all must-fix gates are met；`next_event` 在未送 dispatcher 前保持 null。
3. D final-review package under `/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style` refreshed with source, build artifacts, and truth-pack metadata matching current delivery。
4. Clean `node --run build`, `node --run test:unit`, `node --run test:api`, `node --run test:e2e`, production route probes, browser/cookie auth, protected calendar APIs, AI/apply/rollback, export download probes all rerun and documented。
