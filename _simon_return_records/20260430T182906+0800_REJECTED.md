# Simon QC Verdict — REJECTED

- task_id: `20260428_shift_scheduler_web_supershift_style`
- verified_at: `2026-04-30T18:29:06+08:00`
- active_lock: `PM_OP_QC_REPAIR_LOOP` respected; only active task path processed.
- responsibility: `OP_DELIVERY_DEFECT`
- return_to: `OP / sebastian`

## Verdict

REJECTED. Technical build/live probes are not sufficient for release because the formal delivery gate remains false, the checklist still has unchecked required items, and the standalone API test gate is not clean.

## Required fixes / 必修項

1. Complete or formally reconcile every remaining unchecked `FULL_BUILD_CHECKLIST.md` item; current raw count is `[x]=59`, `[ ]=8`, P0 unchecked=0.
2. Fix standalone `node --run test:api` so it exits 0 cleanly from a fresh shell; current run failed with 18 failed staff API tests because the embedded server at `http://localhost:3101/` did not become ready within 30000ms.
3. Remove/reconcile AI conversation persistence warning: tests emit `[upsertConversation] H64: AICONversation model not in schema — stub called`, while PRODUCT_SPEC requires AI conversations / messages persistence.
4. Complete the remaining product gaps explicitly listed in checklist: named-pattern offset adjustment UI/API, skill/certification constraint parsing, calendar coverage-alert badge rendering, AI solver pipeline beyond mock, RBAC matrix tests, migration deploy path, and single integrated handoff closure.
5. Refresh Windows-visible final-review package under `D:\WORK\成品區\待最終審核\sebastian\20260428_shift_scheduler_web_supershift_style` after the above fixes and synchronize product metadata/truth pack.

## Evidence / 實測證據

- Read required files: `PRODUCT_SPEC.md`, `README.md`, `TEST_RESULT.md`, `TASK_META.json`, `FULL_BUILD_CHECKLIST.md`.
- Current truth flags before verdict: `TASK_META.status=returned_for_fix`, `all_must_fix_completed=false`, `ready_for_build_ready=false`, `next_event=null`; `TEST_RESULT.md` also says `all_must_fix_completed=false` and `ready_for_build_ready=false`.
- Raw checklist count at verdict time: `[x]=59`, `[ ]=8`, P0 unchecked=0.
- Independent subagent technical verification: `node --run build` exit 0; `node --run test:unit` 19/19 pass; `node --run test:e2e` 11/11 pass; live login/auth/calendar/AI preview/apply/export probes passed; dynamic route build warnings recorded but not treated as build failure.
- Main QC clean `node --run test:api` from `/tmp/shiftops_qc` symlink: exit_code=1, 9 suites total, 1 failed / 8 passed, 132/150 tests passed, 18 failed in `tests/api/staff.test.ts` due `Server at http://localhost:3101/ did not become ready within 30000ms`.
- Test stdout includes business persistence warning: `[upsertConversation] H64: AICONversation model not in schema — stub called. Conversation persistence deferred until schema update.` This matches unchecked checklist H64 and cannot be accepted as clean persistence evidence.
- D package found at `/mnt/d/WORK/成品區/待最終審核/sebastian/20260428_shift_scheduler_web_supershift_style`; executable contents present: `{'package.json': True, 'app': True, 'public': True, '.next': True, 'node_modules': True}`. D checklist counts: `[x]=56, [ ]=11, size=11893`. Source checklist size=14684, D checklist size=14742; package/truth metadata requires refresh after current rejection evidence.

## Remaining unchecked checklist lines

- L23: - [ ] 命名制度只作 seed，可調整 phase/天數/班別/組別 offset。→ `NamedPatternSeed` model 存在 (`schema.prisma:253-259`) 但無 UI/API 調整機制；`seed.ts` 只建立 seed。Blocker: "No API/UI to adjust named pattern phase/days/shift/group offset"
- L26: - [ ] 可處理技能/資格/角色覆蓋 constraint。→ `parseIntent` 無技能/資格解析；`buildCalendarProjection` 不考慮 `StaffSkillCertification`；Mock AI 固定 staff assignment。Blocker: "parseIntent skill/certification constraint parsing not implemented"
- L38: - [ ] 缺班警告內嵌在月曆。→ `CoverageAlert` model 存在；Mock AI 產生 `coverageAlerts` (lines 125-135)；但月曆 UI 無 badge 渲染 logic。Blocker: "CoverageAlert model+seed exists; badge rendering in calendar UI not implemented"
- L64: - [ ] AI conversations 留存。→ 無 `AICONversation` model；`AuditEvent` model 存在但只用於 swap/apply 事件。Blocker: "No AICONversation model in schema.prisma"
- L75: - [ ] `AI_SOLVER_SPEC.md` 實作完成。→ Spec file (106 行) 存在；`lib/mock-ai.ts` 實作 parseIntent + buildCalendarProjection；但 `solve()` 函式/SolverInput interface 未實作；Constraint scoring pipeline 未實作（只做 mock）。Blocker: "Solver pipeline (sections 2-5 of AI_SOLVER_SPEC.md) not implemented beyond mock; deterministic solver missing"
- L78: - [ ] `RBAC_MATRIX.md` 權限測試通過。→ Spec file (48 行) 存在；`lib/rbac.ts` 實作基礎 RBAC；但 matrix 測試 (section 4) 無獨立測試檔；member AI scope / manager cross-location blocking 未驗證。Blocker: "No standalone RBAC matrix test suite; lib/rbac.ts basic logic exists but full matrix not tested"
- L79: - [ ] `PRISMA_SCHEMA_SPEC.md` 對應 migration 完成。→ Spec file (336 行) 存在；`schema.prisma` 包含所有 spec models；但無 migrations folder (使用 `db push`)；需確認 `prisma migrate deploy` 可達到同樣 state。Blocker: "No migrations/ folder; db push used instead of migrate; production deployment migration path not verified"
- L84: - [ ] 不拆工、不分批交付。→ 待處理：命名制度 offset 調整 UI (C23)、技能/資格 constraint 解析 (C26)、月曆 UI badge 渲染 (D38)、AICONversation model (H64)、RBAC matrix 獨立測試套件 (I78)、migration deploy path 驗證 (I79)。本輪已補齊：D42、J86、J87。Blocker: "6 genuine feature gaps remain"

## Responsibility / 退回

- responsibility: `OP_DELIVERY_DEFECT`
- return_to: `OP / sebastian`

## Resubmit conditions

- `node --run build`, `node --run test:unit`, `node --run test:api`, `node --run test:e2e` all pass with exit_code=0 and without server lifecycle/open-handle/persistence warnings.
- `FULL_BUILD_CHECKLIST.md`, `TEST_RESULT.md`, `RC.md`, `NEXT_STEP.md`, and `TASK_META.json` are synchronized and truthfully show all must-fix/checklist gates complete before any `build.ready`.
- Production live/browser probes still pass after a clean rebuild.
- D final-review package contains the latest executable build plus synchronized metadata/truth pack.
