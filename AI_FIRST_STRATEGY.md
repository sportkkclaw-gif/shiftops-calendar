# 班表中樞 ShiftOps Calendar — AI-First 產品策略建議
## 當 AI 編排能力加入後，哪些該重寫、哪些該保留

---

## 一、核心理念：AI-first 不是把 AI 當附加功能

現有規格的核心問題：把 AI 當成「規則建構器的強化版」，導致：
- `/builder`（規則建構器）變成另一套 CRUD，AI 只是在上面鍍金
- Rule Engine 仍是「人寫規則 → AI 套用」的舊範式
- AI 沒有自己的入口，只是藏在庫存裡

**重新定位：AI 是排班的主要互動介面，不是輔助工具。**

人類的任務從「設計規則」變成「描述意圖」，AI 負責生成、預演、優化、收斂。

---

## 二、AI-first 版功能架構

### 2.1 頁面/功能重新分組

| 現有頁面 | AI-first 動作 | 結果 |
|---------|-------------|------|
| `/builder` + `/builder/templates` | 合併進「AI 排班助理」單一頁面 | 規則建構器廢止 |
| `/calendar/shift/new` + `/calendar/shift/:id` | AI 對話式新增，或點擊即新增 | 傳統 Modal 簡化 |
| `/reports/coverage` + `/reports/hours` | AI 直接口語回答「這個月缺口多少」 | 報告頁降級 |
| `/swap-requests` | AI 預判並自動提調班建議 | 申請流程加速 |
| `/alerts` | AI 主動在月曆上標記，無需獨立頁面 | Alerts 頁收斂 |
| `/export` + `/export/history` | AI 一鍵匯出，口語指令範圍 | Export 頁簡化 |
| `/staff` + `/shifts` + `/holidays` | 保留，但改為「資料底座」而非独立功能頁 | 降為側邊欄附屬 |
| `/settings` + `/billing` | 保留 | 無變化 |

### 2.2 新的核心頁面架構（AI-first）

```
/calendar              ← 月曆主體 + AI 浮動按鈕（主要入口）
/ai-assist             ← AI 排班助理（對話式，取代 builder）
/data/staff            ← 人員資料（側邊欄附屬）
/data/shifts           ← 班別資料（側邊欄附屬）
/data/holidays         ← 假日資料（側邊欄附屬）
/swap-requests         ← 調班審核（仍需人類把關）
/reports               ← 報告（降級，口語查詢為主）
/export                ← 匯出（簡化，AI 一鍵觸發）
/settings              ← 設定（保留）
/billing               ← 訂閱（保留）
```

---

## 三、可被 AI 取代的功能（廢止或收斂）

### 3.1 規則建構器（`/builder` 全頁）

**取代方式：**
- 使用者說「這個月A班每週一二三四上班，B班五六日」→ AI 理解為規則並預覽
- 使用者說「做三休一，輪替至月底」→ AI 生成完整排班表並預演
- 使用者說「避開所有國定假日，自動跳過」→ AI 遵守 + 標記跳過日

**廢止原因：**
- 規則建構器本質是人類在教系統做事，AI 時代應反過來
- 現有 Weekly Pattern、A/B Rotation、N-days Cycle 全可被自然語言取代
- 模板庫（做三休一、A/B 輪替等）變成 AI 的內建知識，無需使用者選

**保留元件：**
- 規則預覽（preview）是必要的，但改為 AI 預演結果展示，不是独立頁面

### 3.2 獨立報告頁（`/reports/coverage`、`/reports/hours`）

**取代方式：**
- 使用者說「這個月的工時分佈？」→ AI 直接口語回答 + 附圖表
- 使用者說「缺口在哪些天？」→ AI 在月曆上直接打標記

**廢止原因：**
- 報告頁是「人去系統找資料」，AI 時代應是「系統主動呈現」
- Coverage Engine 的計算邏輯保留（後端），但前端展示被 AI 對話取代

**保留元件：**
- 純數字統計（本月工時總計、A/B班天數）仍可做成小工具卡，但不需独立頁面

### 3.3 異常清單獨立頁（`/alerts`）

**取代方式：**
- AI 在月曆 cell 上直接顯示紅色警告 icon
- 點擊 icon 可看到異常明細（與 Day Inspector 整合）

**廢止原因：**
- 独立 alerts 頁是主動式系統的舊設計；AI 時代異常應該被主動整合進工作流

**保留元件：**
- Coverage Engine 的計算邏輯完整保留（後端 alert generation 不變）
- 「已處理/未處理」狀態追蹤保留（資料層）

### 3.4 傳統 Shift Modal（新增/編輯班次）

**取代方式：**
- AI 對話：「幫我新增明天A班張三」
- 或直接點擊月曆空格 → AI 理解上下文（最近班別、人員偏好）自動補全

**廢止原因：**
- Modal 是一種「系統主導」的互動，AI 時代應該「人類說意圖，系統執行」

**保留元件：**
- 快速手動調整仍需要（但不一定要完整 Modal，可以是 inline edit）
- 「批量新增」場景仍需要，但改由 AI 處理（「幫我這個月每天A班都排張三」）

---

## 四、不可被 AI 取代的核心（必須保留為資料/審核/安全底座）

### 4.1 人員資料 (`/data/staff`)
- **原因：** 身份驗證、RBAC 權限、薪資基礎資料不能被 AI 生成
- **保留什麼：** 基本 CRUD、角色設定、在職狀態
- **AI 角色：** 加速新增（「新增三名新員工」），但資料源頭仍是人類確認

### 4.2 班別設定 (`/data/shifts`)
- **原因：** 班別（A班/B班/休息）是業務定義，影響法規、工時、保險
- **保留什麼：** 班別名稱、顏色、時間區間、法定工時檢查
- **AI 角色：** 學習班別偏好，自動建議排班，但班別本身不可被 AI 發明

### 4.3 假日與農曆 (`/data/holidays`)
- **原因：** 法律強制性，必須精確
- **保留什麼：** 假日 CRUD、農曆 seed、未來年度擴充
- **AI 角色：** 自動避開，但資料本身不能被 AI 篡改

### 4.4 調班審核 (`/swap-requests`)
- **原因：** 人類情感、階級關係、突發事件需要人類判斷
- **保留什麼：** 申請 → 審核 → 通過/拒絕 的完整流程
- **AI 角色：** 自動偵測衝突並預提示警，但不替代人類核准

### 4.5 匯出功能 (`/export`)
- **原因：** PDF/ICS/Google Calendar 是外部系統合約，不能靠 AI 隨機生成
- **保留什麼：** 格式標準、檔案完整性、匯出歷史追蹤
- **AI 角色：** 一鍵觸發、口語指定範圍（「匯出三月PDF」），但檔案本身按規格生成

### 4.6 資料安全與Audit
- **原因：** 所有資料變更需要留下軌跡，確保合規
- **保留什麼：** `activity_logs`、RBAC 檢查、審計事件寫入
- **AI 角色：** AI 操作的動作同樣寫入 audit log，不能有 exempt

---

## 五、規格重寫方向

### 5.1 新增 AI 模組（原本沒有）

```
## AI排班引擎（AIAgent）
- 自然語言解析：把「做三休一」轉為內部規則表達
- 多意圖識別：同時處理「排班 + 避開國定假日 + 最小化加班」
- 預演模式：生成後不直接寫入，等待人類確認
- 衝突仲裁：同時滿足多人偏好時，報告 trade-off 讓人類選
- 可解釋性：每個 AI 產生的班次需附「原因是...」

## AI月曆助理（AIAssistant）
- 浮動按鈕：月曆右下角，點開對話視窗
- 上下文感知：知道當前查看的月份、人員名單、已知偏好
- 主動預警：每週主動總結「本週缺口”和「下週注意」
```

### 5.2 移除/簡化的功能章節

```
## 移除章節
- 規則建構器 UI（前端完全移除）
- 規則模板庫（改為 AI 內建知識）
- 獨立 Alerts 頁（改為月曆內嵌）

## 簡化章節
- Shift Modal：简化为「快速新增」+ AI 對話補充
- Report 頁：從獨立頁降為 AI 回應 + 可選詳細檢視
```

### 5.3 規格章節重新排序

```
新的 SPEC.md 章節順序：
1. 產品定位（AI-first 排班工具）
2. 技術棧（不變）
3. AI 排班引擎規格（新增，優先）
4. 資料模型（staff, shift, holiday — 不變）
5. 資料庫（不變，但加入 ai_generation_log）
6. API：新增 /api/ai/generate, /api/ai/preview, /api/ai/revise
7. 頁面架構（AI-first 版本）
8. 規則引擎（降級，只保留後端計算邏輯，前端規則 UI 移除）
9. Coverage Engine（保留後端，前端改 AI 呈現）
10. Export（保留，簡化 UI）
11. Swap（保留人類審核）
12. 安全與審計（不變，增加 AI 操作 audit）
```

---

## 六、開發優先順序建議

### Phase 1：資料底座（不可省）
1. Auth/RBAC
2. 人員 CRUD
3. 班別 CRUD
4. 假日/農曆 seed
5. 月曆主體顯示

### Phase 2：AI 核心（區分新舊）
6. AI 排班引擎（自然語言 → 班次結構）
7. AI 預演 + 人類確認寫入
8. AI 助理浮動對話框

### Phase 3：收斂舊功能
9. 移除規則建構器前端（後端 Rule Engine 保留給 AI 呼叫）
10. Alerts 整合進月曆
11. 報告頁降級為 AI 口語回應 + 備用詳細頁

### Phase 4：完整交付
12. Export（含 AI 一鍵觸發）
13. Swap 流程（完整人類審核）
14. PWA + seed + 測試

---

## 七、總結：哪些該重寫

| 項目 | 動作 | 原因 |
|------|------|------|
| `SPEC.md` 第6節 Rule Engine | 改為「Rule Engine 為 AI API」 | 前端規則 UI 廢止 |
| `SPEC.md` 第7節 Coverage Engine | 保留，標記「給 AI 用」 | 計算邏輯不變，展示層由 AI 接管 |
| `proposal_full.md` 第3節頁面架構 | 重新繪製（AI-first 版） | 16+ 頁面 → 10核心頁 |
| `FULL_BUILD_CHECKLIST.md` C 節規則建構器 | 重新定義 | 從「建構器 UI」改為「AI 規則解析」 |
| 新增 `AI_ENGINE_SPEC.md` | 必須新增 | 原本完全沒有 AI 規格章節 |
| `UI_LAYOUT_SPEC.md` | 移除 `/builder` 頁設計，新增 AI Assistant UI 規格 | 規則建構器頁廢止 |

---

## 八、已確認不能被 AI 取代的紅線

1. **身份與權限**：登入、RBAC、資料擁有權判斷
2. **法規資料**：假日、農曆、班別時間（法律強制性）
3. **人類審核**：調班核准、異常最終處置
4. **資料安全**：所有變更的 audit log（含 AI 操作本身）
5. **匯出合約**：PDF/ICS/Google Calendar 格式標準
6. **帳務邏輯**：工時統計、加班計算（涉及法規）

---

**產出狀態：** 策略建議完成。需 Sebastian 確認方向後，修訂 `SPEC.md`（AI-first 版）、重寫 `FULL_BUILD_CHECKLIST.md`，並新增 `AI_ENGINE_SPEC.md`。